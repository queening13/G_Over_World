# G-Over World — 재현 가이드

구식 CGI 게시판 게임 양식(judomst.net 계열)의 **MS 파일럿 육성 시뮬레이션**.
FF ADVENTURE 계열 **2d6 대항판정**으로 전투를 굴리고, 기체 제원·무장·이미지·**지형 적성**은
**GGen Eternal Database** 공개 API에서 가져온 실제 데이터를 쓴다.

전투는 **전장(우주·공중·지상·수상·수중) → 교전 거리(1~20) → 색적 → EN** 순으로
상황이 잡히고, 각성 800 이상이면 **미래 예측**, HP 30% 이하에서는 **저력**이 발동한다.
파일럿은 능력치 6종(상한 999) · **스킬 17종** 을, 기체는 **자체 레벨(최대 9999)** 을 따로 키운다.

**배포본 : https://g-over-world.vercel.app**

수록 기체 **1244기 전부**. **Node.js 서버 애플리케이션**이다 —
게임 규칙과 세이브는 서버가 갖고, 브라우저는 화면만 그린다.
세이브 저장소는 **MongoDB** 또는 로컬 파일 중에서 고른다.

```bash
npm install && node src/fetch-roster.mjs && node build.mjs && node server.mjs
```

> **v5.0 에서 구조가 바뀌었다.** 그전까지는 `docs/index.html` 한 장에 규칙까지
> 다 들어간 순수 클라이언트 앱이었고 세이브는 `localStorage` 에 있었다.
> 지금은 규칙이 `src/rules.mjs` 로 빠져 서버에서 돌고, 브라우저는 액션을
> 보내고 결과를 받아 그리기만 한다. **게임 규칙 자체는 한 줄도 바꾸지 않았다**
> ([§5.4 이관 검증](#54-이관이-정확한지-어떻게-확인했나) 참조).

---

## 목차

1. [5분 재현](#1-5분-재현)
2. [요구 사항](#2-요구-사항)
3. [디렉터리 구조](#3-디렉터리-구조)
4. [데이터 파이프라인](#4-데이터-파이프라인)
4-5. [한글화](#45-한글화)
5. [서버 구조](#5-서버-구조)
6. [실행 · 배포(로컬 · MongoDB · Vercel)](#6-실행과-배포)
7. [게임 설계 명세](#7-게임-설계-명세)
8. [전투 규칙 전문](#8-전투-규칙-전문)
9. [밸런스 — 상수와 근거](#9-밸런스--상수와-근거)
10. [밸런스 검증 시뮬레이터](#10-밸런스-검증-시뮬레이터)
11. [함정 모음](#11-함정-모음)
12. [스타일 가이드](#12-스타일-가이드)
13. [커스터마이즈 레시피](#13-커스터마이즈-레시피)
14. [출처와 권리](#14-출처와-권리)

---

## 1. 5분 재현

빈 디렉터리에 `package.json` `build.mjs` `server.mjs` `vercel.json`,
`api/[...path].mjs`, `test/`, 그리고 `src/` 의 일곱 파일
(`head.html` `rules.mjs` `actions.mjs` `api.mjs` `store.mjs` `client.js`
`i18n.mjs` `fetch-roster.mjs`)을 놓은 뒤:

```bash
npm install
```

```bash
node src/fetch-roster.mjs
```

```bash
node build.mjs
```

```bash
node server.mjs
```

`http://localhost:8788/` 로 접속하면 끝. 수집은 1244기 기준 **약 5분**
(상세 1244건 + 이미지 2488장 / 142MB)이고, 빌드는 즉시 끝난다.
세이브는 `data/` 폴더에 쌓인다 — MongoDB 로 옮기려면
[§6.2](#62-mongodb-로-옮기기) 참조.

빠르게 확인만 하려면 등급별 3기씩만 받는다:

```bash
node src/fetch-roster.mjs --limit=3 && node build.mjs
```

> **로스터 수집은 결정적(deterministic)이다.** 선정 로직에 난수를 쓰지 않으므로
> 같은 API 상태에서 두 번 돌리면 `src/roster.json` 이 바이트 단위로 동일하게 나온다.

---

## 2. 요구 사항

| 항목 | 버전 / 비고 |
|---|---|
| Node.js | **20 이상** (서버가 최상위 `await` 를 쓴다). 검증 환경은 v20.15.0 |
| 인터넷 | 최초 로스터 수집 때만. 이후 빌드·실행은 오프라인 가능 |
| 브라우저 | 최신 Chrome / Edge / Firefox. 쿠키 필요(세션 식별) |
| 디스크 | 약 150MB (초상화 133MB + 썸네일 9.3MB + 나머지) |
| **mongodb** | 세이브를 MongoDB 에 둘 때. `package.json` 의 유일한 런타임 의존성 |
| sharp | **선택.** `--resize` 로 초상화를 줄일 때만 필요 |

MongoDB 를 안 쓸 거면 `npm install --omit=optional` 없이도 무방하다 —
`MONGODB_URI` 가 없으면 드라이버를 아예 `import` 하지 않고 파일 저장소로 간다.

---

## 3. 디렉터리 구조

```
.
├── server.mjs             Node HTTP 서버 — /api 는 src/api.mjs, 나머지는 docs/
├── build.mjs              docs/index.html · docs/app.js 생성
├── package.json           npm start / build / fetch / sim / test
├── vercel.json            Vercel 배포 설정 (함수 라우팅·캐시 헤더)
├── api/
│   └── [...path].mjs      Vercel 서버리스 진입점 — 파일명이 곧 /api/* 라우트
├── .env.example           환경변수 서식 (.env 로 복사해서 쓴다)
├── test/
│   ├── sim.mjs            밸런스 시뮬레이터 (서버 없이 전투만 대량 실행)
│   └── e2e.mjs            API 종단 점검 (정상 동작 + 변조 요청 거절)
├── data/                  파일 저장소일 때 세이브가 쌓이는 곳 (git 제외)
├── docs/                  정적 자산 — 그대로 CDN 에 올려도 된다
│   ├── index.html         0.02MB — 골격 + CSS 만
│   ├── app.js             1.06MB — 로스터 + 규칙 + 클라이언트
│   ├── img/               초상화 936x803 · 1244장 · 133MB
│   └── th/                썸네일 128x128 · 1244장 · 9.3MB
└── src/
    ├── fetch-roster.mjs   GGen DB API → roster.json + docs/img · docs/th
    ├── i18n.mjs           수집 시점 한글화 사전 (무장·특성·시리즈)
    ├── roster.json        기체 1244기 데이터 (0.95MB, 이미지는 경로만)
    ├── head.html          <title>·CSS·마크업 골격
    ├── rules.mjs          게임 규칙 — 상수·판정·전투. 서버·클라 공용
    ├── actions.mjs        액션 처리와 검증 (서버 전용)
    ├── api.mjs            HTTP 라우팅·세션 (서버 전용)
    ├── store.mjs          세이브 저장소 — mongo / file (서버 전용)
    └── client.js          렌더링 · 전투 재생 · 액션 전송 (브라우저 전용)
```

**서버 전용 세 파일(actions·api·store)은 브라우저 번들에 들어가지 않는다.**
검증 로직이 클라이언트로 새면 서버에 두는 의미가 없다.

---

## 4. 데이터 파이프라인

### 4.1 API 개요

베이스: `https://ggendb.up.railway.app`
인증 없음. JSON. 언어는 `lang=` 로 지정하며 **EN / TW / HK / JP** 만 지원한다
(한국어 없음 → 기체명은 영문, UI만 한글).

#### 목록 — `GET /api/units`

```
/api/units?lang=EN&page=1&per_page=100&grid_skills=1
```

| 파라미터 | 설명 |
|---|---|
| `page` | 1부터 |
| `per_page` | **서버가 100으로 자른다.** 200을 넣어도 100만 온다 |
| `sort` / `dir` | `rarity` 등, 선택 |
| `q` | 이름 검색, 선택 |

응답은 래퍼 객체이고 실제 유닛 배열은 **`rows`** 에 들어 있다.
`total`(1244), `total_pages` 를 보고 순회할 것.

`rows[]` 항목:

| 필드 | 예 | 설명 |
|---|---|---|
| `id` | `"1001000150"` | 상세 조회 키 |
| `name` | `"Gundam (EX)"` | 표시명 |
| `rarity` | `N` `R` `SR` `SSR` `UR` | 등급 |
| `role` | `Attack` `Defense` `Support` | 역할 |
| `HP` `ATK` `DEF` `MOB` `EN` `MOV` | 151599 / 10886 / 10040 / 8978 / 399 / 5 | 제원 |
| `thum` | `https://…github.io/…/thum_g0010u00150.webp` | 썸네일 URL (44px용, 미사용) |
| `grid_abilities[]` | `{name, detail, icon}` | 특성 요약 |

#### 상세 — `GET /api/unit/<id>`

```
/api/unit/1001000150?lang=EN
```

목록에 없는 것들이 여기 있다:

| 필드 | 설명 |
|---|---|
| `model` | `"RX-78-2"` 형식번호 |
| `series[]` | `{id, name, icon}` — `[0].name` 이 작품명 |
| `portrait` | **기체 초상화 URL.** 이 프로젝트가 쓰는 이미지 |
| `is_large` | 대형기 여부 |
| `abilities[]` | `{display_name, details:[{text}], icon}` |
| **`weapons[]`** | 아래 참조 |

`weapons[]` 항목:

| 필드 | 설명 |
|---|---|
| `name` | 무장명 |
| **`levels[]`** | `{level, power, en, ammo, accuracy, critical}` — **레벨별 위력표.** 무장 강화 시스템의 근거 |
| `power` `accuracy` `critical` | 대표값 |
| `en_cost` `ammo` | 소모 자원 |
| `min_range` `max_range` | 사거리 |
| `attribute` | `"Beam"` `"Physical"` 등 |
| `attack_types[]` | `{key:"melee"\|"shooting", label}` |
| `is_map` `is_ssp_weapon` `is_preemptive` | 분류 플래그 |

> 다른 URL 형태(`/api/units/<id>`, `/api/unit_detail?id=`)는 **404**다.
> 단수형 `/api/unit/<id>` 만 동작한다.

이미지는 별도 호스트(GitHub Pages)에 있다:
`https://zh7tcm9fmv-cloud.github.io/ggen_db_images/…`

**규격은 딱 두 가지뿐이고 중간 크기는 없다.**

| 경로 | 크기 | 용량 |
|---|---|---|
| `images/Trait/thum/thum_<code>.webp` | 작은 아이콘 | 약 8.8KB |
| `images/unit_portraits/ub_<code>.webp` | 936×803 초상화 | 약 140~226KB |

썸네일 URL에서 초상화 URL로 가는 규칙은 경로 중간만 바꾸면 된다:
`/Trait/thum/thum_` → `/unit_portraits/ub_`.
상세 응답의 `portrait` 필드에 완성된 URL이 이미 들어 있으므로 그쪽을 먼저 쓰고,
없을 때만 문자열 치환으로 만든다.

### 4.2 로스터 선정

기본은 **전 기체(1244기)** 다. 데이터만 보면 기당 740B 라 전부 담아도 0.92MB 밖에 안 된다.

```
UR 114 · SSR 350 · SR 365 · R 313 · N 102  =  1244기
```

`--limit=<n>` 을 주면 등급별로 `n` 기씩만 균등 간격으로 뽑는다. 개발 중 빠른
확인용이며, 앞에서부터 순서대로가 아니라 목록 전체를 훑는 간격 샘플이라
시리즈가 한쪽에 쏠리지 않는다.

---

### 4.3 저장 스키마

용량을 아끼려고 필드명을 짧게 줄인다. `roster.json` 은 이 객체의 배열이다.

```js
{
  id, nm,        // id, 이름 ('[SD] ' 접두어 포함)
  mdl, sr,       // 형식번호(model), 시리즈명(series[0].name)
  rar,           // 등급 — 화면에는 안 나온다. 적 편성 풀·EXP 계산용 내부 키
  hp,            // 원본 그대로 (상한 999,999)
  atk, def, mob, // 원본의 10%, 소수점 버림 (상한 9,999)  — #19 · #20
  en,            // 원본 그대로 (상한 999)
  sct,           // 색적 — 원 데이터에 없어 파생시킨 값 (상한 9,999)  — #11
  tr: { sp, ai, gr, se, uw },   // 지형 적성 0=× / 1=△ / 2=○        — #15
  lg,            // is_large
  w: [{          // 무장 (최종 레벨 위력 내림차순, 최대 6종)
    n,           // 이름
    pw: [ ... ], // 레벨별 위력 배열. pw[0]=Lv1, pw[len-1]=최대
    ac, cr,      // accuracy, critical
    en, am,      // EN 소모, 탄수 (0이면 무제한)
    mn, mx,      // 최소/최대 사거리 (표시용)
    at,          // 속성 "빔" / "실탄" / "특수" (한글화됨)
    tp,          // "M"=격투 / "S"=사격
    pre          // 선제 공격 플래그
  }],
  ab: [{ n, d }],// 특성 이름 + 설명(원문 160자까지 읽어 한글화)
  img,           // 'img/<id>.webp'  초상화 936x803 — 전투·도시에·도감 상세용
  th             // 'th/<id>.webp'   썸네일 128x128 — 목록·그리드용
}
```

`role`(공격형·방어형·지원형)과 `mov`(이동력)는 **저장하지 않는다.**
역할 구분은 게임에서 완전히 제거했고(#9), 이동력은 색적을 파생시킬 때만 쓰고
버린다(#11). 두 필드를 지우면 `roster.json` 이 약 20KB 줄어든다.

**맵 병기(`is_map`)와 SSP 전용 병기(`is_ssp_weapon`)는 제외한다.** 밸런스가
잡히지 않는 특수 무장이라 그대로 넣으면 수치만 망가진다.

**지형 적성 변환** — API 의 `terrain[].level` 은 3(●) / 2(▲) / 1(-) 이다.
게임 값은 `level − 1` 을 0~2로 clamp 한 것이라 **●=2 / ▲=1 / -=0** 이 된다.

### 4.4 이미지는 왜 분리하는가

한때 base64 로 HTML 에 내장했었다. 단일 파일이라는 장점이 있었지만 대가가 컸다.

| | 내장(base64) | 분리(파일) |
|---|---|---|
| 용량 | ×1.34 팽창 | 원본 그대로 |
| 지연 로딩 | 불가 — 전부 파싱 | `loading="lazy"` 로 보이는 것만 |
| 캐시 | HTML 통째로 | 파일 단위 |
| 상한 | Artifact 16MB | 없음 |

1244기를 내장하면 초상화 기준 **약 29MB** 라 아티팩트 상한을 넘고, 도감을 열
때마다 브라우저가 그 전부를 파싱해야 한다. 분리하면 첫 로딩은 `index.html`
0.86MB 뿐이고 이미지는 화면에 들어온 것만 내려간다.

#### 두 규격을 모두 받는 이유

호스트가 주는 규격은 딱 둘이다.

| 경로 | 크기 | 용량 |
|---|---|---|
| `images/Trait/thum/thum_<code>.webp` | 128×128 | 약 8.8KB |
| `images/unit_portraits/ub_<code>.webp` | 936×803 | 약 109KB |

도감 그리드는 44px 칸이다. 여기에 109KB 초상화를 쓰면 스크롤할 때마다 낭비가
크므로 **목록에는 썸네일, 상세·전투에는 초상화**를 쓴다. 경계는 썸네일 해상도가
버티는 지점(128px)에서 갈랐다.

```js
// src/client.js
const picFor = (B, cls) => (cls === 'l' || cls === 'xl') ? B.img : (B.th || B.img);
// .s 44px · .m 72px  → 썸네일
// .l 104px · .xl 132px → 초상화
```

합계 142MB. `--resize=<px>` 를 주면 초상화를 줄일 수 있다(sharp 필요).

| 초상화 폭 | 기당 | 1244기 합계 |
|---|---|---|
| **원본 936px** | **109KB** | **133MB** |
| 512 | 약 45KB | 약 56MB |
| 288 | 약 24KB | 약 30MB |

---

### 4.5 한글화

API는 **EN / TW / HK / JP** 만 준다. 한국어가 없으므로 무장·특성·시리즈는
`src/i18n.mjs` 가 **빌드 시점에** 옮긴다. `roster.json` 에는 이미 한글이 들어가므로
런타임 비용은 0이다.

대상 규모(1244기 기준): 고유 무장 **1226종**, 특성명 **252종**, 특성 설명 **173종**, 시리즈 **103종**.

3단 구조로 처리한다.

1. **완전 일치 표** (`WEP_EXACT`) — 낱말로 쪼개면 망가지는 것. `Kick` → 킥,
   `Sekiha Tenkyoken` → 석파천귀권.
2. **구 치환** (`WEP_PHRASE`, `AB_PHRASE`) — `Beam Rifle` → 빔 라이플.
   **반드시 긴 키부터** 적용한다(`byLenDesc`). 짧은 키가 먼저 걸리면
   `Increased ATK & MOB` 이 `공격력 증가 & MOB` 처럼 반쪽만 번역된다.
3. **낱말 치환** (`WEP_TOKEN`) — 남은 단어. 구분자는 공백·괄호·슬래시만 쓴다.
   **하이픈으로 쪼개면 안 된다** — `Air-to-Air` 가 `공-to-공` 이 되어버린다.

특성 설명은 문형이 정해져 있어 정규식으로 옮긴다.

```
When HP is 75% or above, increase ATK by 20%.
  → HP 75% 이상일 때 공격력 20% 증가.
When enemies attack with a beam ranged weapon, reduce damage taken by 50%.
  → 적이 빔 사격 무장으로 공격할 때 받는 피해 50% 감소.
```

**형식번호·고유명은 일부러 영문으로 남긴다.** `MA-M01 Lacerta`, `RFW-99`,
`"Forfanterie"`, `Okitsu-no-Kagami` 같은 것을 억지로 음차하면 오히려 읽기 나쁘다.

최종 커버리지 — 시리즈 **103/103**, 특성명 **220/252**, 무장 **844/1226**.
무장 잔여 382건은 대부분 형식번호와 일본어 필살기명이다.

> **재번역은 이미지를 다시 받지 않는다.** 사전을 고친 뒤 `--skip-images` 를 주면
> 상세 API 만 다시 돌아 1분이면 끝난다. 133MB 를 다시 받을 이유가 없다.
>
> ```bash
> node src/fetch-roster.mjs --from-cache --skip-images && node build.mjs
> ```

#### 사전 커버리지 확인

사전을 고친 뒤에는 무엇이 안 걸렸는지 확인할 것. 프로젝트 루트에서:

```bash
node --input-type=module -e "import fs from 'node:fs';import{trWeapon,trAbilityName,trAbilityDetail}from'./src/i18n.mjs';const R=JSON.parse(fs.readFileSync('src/roster.json','utf8'));const w=new Set(),a=new Set();R.forEach(u=>{u.w.forEach(x=>/[A-Za-z]{3,}/.test(x.n)&&w.add(x.n));(u.ab||[]).forEach(x=>/[A-Za-z]{3,}/.test(x.n)&&a.add(x.n))});console.log('무장 잔여',w.size);console.log([...w].join('
'));console.log('특성 잔여',a.size)"
```

한글 뒤에 영문이 붙어 있으면(`핀 판넬s`) 구 치환이 반쪽만 걸린 것이다.
`trWeapon` 끝의 `([가-힣])s → $1` 안전망이 복수형은 걷어내지만,
원인이 되는 구를 사전에 추가하는 편이 낫다.

---

## 5. 서버 구조

### 5.1 무엇이 어디로 갔나

| 하는 일 | v4 (클라이언트 단독) | v5 (Node.js 서버) |
|---|---|---|
| 게임 규칙 | `engine.js` (브라우저) | `src/rules.mjs` — **서버가 실행** |
| 액션 검증 | 없음 (클라이언트가 자기 세이브를 직접 고침) | `src/actions.mjs` — 서버가 판단 |
| 세이브 | `localStorage` | MongoDB 또는 `data/*.json` |
| 전투 | 브라우저에서 굴리며 그림 | 서버가 끝까지 계산 → 연출 이벤트로 재생 |
| 화면 | `ui.js` | `src/client.js` (그리기 전용) |

구식 CGI 였다면 클릭 한 번에 `index.cgi?mode=xxx` 로 가서 HTML 한 장을 통째로
받아 왔을 것이다. 여기서는 그 자리를 `POST /api/act` 가 대신하되,
**연출은 잃지 않는다** ([5.3](#53-전투를-서버로-옮기면서-연출을-지킨-방법)).

### 5.2 요청 한 번의 흐름

```
[브라우저] 【훈련】 사격 클릭
    │  act({ k:'train', stat:'sho' })
    ▼
POST /api/act              쿠키 gow_pid 로 플레이어 식별
    │
    ├─ store.get(pid)      세이브를 꺼낸다
    ├─ applyAction(g, a)   ── actions.mjs
    │     · 없는 능력치인가?           → 400
    │     · 올드타입이라 각성이 잠겼나? → 400
    │     · 행동력이 있나?             → 400
    │     · rules.mjs 로 판정하고 g 를 고친다
    ├─ store.put(pid, g)   저장
    ▼
{ g, out:{ res } }         새 세이브 + 판정 결과
    │
    ▼
[브라우저] setState(g) → renderAll()
```

`out.msg` 는 안내 상자에, `out.res` 는 훈련 결과 상자에, `out.battle` 은
전투 재생에 쓰인다. **클라이언트가 세이브를 직접 고치는 코드는 한 줄도 없다.**

### 5.3 전투를 서버로 옮기면서 연출을 지킨 방법

전투를 서버에서 끝까지 굴리면 결과만 남고 라운드별 연출이 사라진다.
그래서 `resolveBattle()` 은 **화면 갱신 순서를 그대로 기록**해서 돌려준다.

```js
{ t:'l', h, c }              로그 한 줄
{ t:'b', a, d, u }           상황판 — 행동 유닛·거리·유닛별 hp/en/은신
{ t:'d', a, x, w, m, k, n }  교전 표시 — 공격/피격·무장·피해·종류·대사
{ t:'w', ms }                연출 대기
```

클라이언트는 이 배열을 순서대로 훑으며 `bl()` · `paintBoard()` · `paintDuel()` 을
부르고 `{t:'w'}` 에서 `await sleep(ms)` 한다. 즉 **v4 의 `runBattle()` 이 하던 일을
그대로, 다만 판정 없이** 반복한다. 【연출 생략】 버튼도 그대로 먹는다.

한 전투의 크기는 실측 **15KB / 이벤트 180개** 정도다. 유닛 이름·초상화 같은
정적 정보는 `cast` 로 한 번만 보내고, 이후 이벤트는 인덱스로만 가리킨다.

### 5.4 이관이 정확한지 어떻게 확인했나

"게임 방식을 그대로 유지"가 요구사항이었으므로 **승률 비교로는 부족하다.**
1244기 로스터에서 적 편성이 매번 달라지기 때문에 N=150 으로 돌려도
같은 엔진끼리 57%~70% 를 오간다. 실제로 신·구 승률 차이는 −2.7%p 에서
+13.3%p 까지 흩어졌고 부호도 뒤섞였다 — 이걸로는 아무것도 증명하지 못한다.

그래서 **결정론적 함수를 1:1 로 대조**했다. 구 `engine.js` 를 `vm` 컨텍스트에
올리고, 신 `rules.mjs` 와 같은 입력·같은 난수열을 먹인 뒤 출력을 비교한다.

| 대조 항목 | 건수 |
|---|---|
| `uStat` · `effStats` · `penMul` · 지형 적성 · 가격 · 암시장 | 360 |
| `mkPlayer` (능력치·전법·지형·무장 사거리·EN) | 60 |
| `hitAdv` · `calcDmg` · `critNeed` · `armed` · `chooseWep` · 색적/이동 보정 | 80세트 |

스킬 6종 조합 × 전법 4종 × 전장 5종 × 정예/지휘기 × 저력 × 사거리 밖 상태를
돌려 **불일치 0건**. 규칙은 옮겨지기만 했고 바뀌지 않았다.

### 5.5 빌드

```bash
node build.mjs      # 또는 npm run build
```

* `docs/index.html` ← `src/head.html` 에서 인라인 `<script>` 를 떼고
  `<script src="app.js" defer>` 를 건다
* `docs/app.js` ← `로스터` + `rules.mjs` + `client.js`

`rules.mjs` 는 서버에서 `import` 하는 ES 모듈이지만 브라우저에는 번들러 없이
이어 붙인다. 그래서 맨 끝 `export` 블록을 잘라내는데, **잘라내는 지점 표시는
반드시 한 줄짜리 주석이어야 한다** ([§11.21](#1121-여러-줄-주석-한가운데를-자르면-뒤가-통째로-먹힌다)).

`rules.mjs` 와 `client.js` 는 **같은 스코프를 공유한다.** `client.js` 가
`g` 를 그대로 읽을 수 있는 이유이자, `client.js` 에서 `g` 를 다시 선언하면
안 되는 이유다. 값을 바꿀 때는 `setState()` 를 쓴다.

---

## 6. 실행과 배포

### 6.1 로컬 실행

```bash
npm install
node build.mjs
node server.mjs                       # 또는 npm start  →  http://localhost:8788/
PORT=3000 node server.mjs
node --env-file=.env server.mjs       # .env 에서 환경변수를 읽는다 (Node 20.6+)
```

세이브는 `data/<플레이어코드>.json` 에 쌓인다. 임시 파일에 쓰고 `rename` 하므로
쓰다 죽어도 세이브가 반쯤 남지 않는다.

`.env.example` 을 `.env` 로 복사해 쓰면 된다. `.env` 는 `.gitignore` 에 있다.

```bash
cp .env.example .env
```

> **`docs/index.html` 을 브라우저로 직접 열면 안 된다.** v4 까지는 됐지만
> 이제 `/api/*` 가 필요하다. 반드시 서버를 거칠 것.

점검:

```bash
node test/e2e.mjs          # 서버를 띄운 상태에서. 정상 동작 + 변조 요청 거절
node test/sim.mjs          # 밸런스 시뮬레이터 (서버 불필요)
```

### 6.2 MongoDB Atlas 붙이기

무료 티어 **M0(512MB)** 면 충분하다. 세이브 하나가 **0.7KB** 라 5인이 몇 년을
놀아도 남는다. 카드 등록도 필요 없다.

#### 6.2.1 클러스터 만들기

1. [cloud.mongodb.com](https://cloud.mongodb.com) 가입 / 로그인
2. 프로젝트를 하나 만든다 (이름은 아무거나 — `G-Over World`)
3. **Create / Build a Database** → 요금제에서 **M0 (Free)** 선택
4. 클라우드 제공자와 리전을 고른다
   * 한국에서 쓸 거면 **AWS · Seoul (ap-northeast-2)**
   * Vercel 에 올릴 거면 Vercel 함수 리전과 가까운 쪽이 지연이 적다
5. 클러스터 이름을 정하고 생성 (1~3분 걸린다)

#### 6.2.2 접속 계정 만들기

**Database Access → Add New Database User**

| 항목 | 값 |
|---|---|
| 인증 방식 | Password |
| 사용자명 | 예 `gover` |
| 비밀번호 | **Autogenerate 를 눌러 만든 값을 그대로 쓰는 편이 낫다** |
| 권한 | `Read and write to any database` (좁히려면 `gover` DB 에만) |

> **비밀번호에 특수문자가 있으면 연결 문자열에서 퍼센트 인코딩해야 한다.**
> 이걸 빠뜨려서 인증 실패가 나는 경우가 가장 흔하다.
>
> | 문자 | `@` | `#` | `/` | `:` | `?` | `&` | `%` |
> |---|---|---|---|---|---|---|---|
> | 인코딩 | `%40` | `%23` | `%2F` | `%3A` | `%3F` | `%26` | `%25` |
>
> 애초에 특수문자 없는 비밀번호를 쓰면 이 문제를 피할 수 있다.

#### 6.2.3 접속 허용 IP

**Network Access → Add IP Address**

* 로컬에서만 쓸 거면 **Add Current IP Address**
* **Vercel 에 올릴 거면 `0.0.0.0/0` (Allow Access from Anywhere)**
  — Vercel Hobby 는 함수 IP 가 고정되지 않는다. 계정·비밀번호로만 막히는
  상태가 되므로 **비밀번호를 길게 쓸 것.**

#### 6.2.4 연결 문자열

**Database → Connect → Drivers → Node.js** 에서 문자열을 복사한다.

```
mongodb+srv://gover:<db_password>@cluster0.abcde.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
```

`<db_password>` 를 실제 비밀번호로 바꾼다. `appName` 은 남겨도 무방하다.

#### 6.2.5 로컬에서 먼저 확인한다 — 이 단계를 건너뛰지 말 것

**Vercel 에 올리기 전에 로컬에서 Atlas 로 한 판을 돌려 본다.** 그래야 나중에
실패했을 때 원인이 Atlas 쪽인지 Vercel 쪽인지 바로 갈린다.

```bash
cp .env.example .env         # 그리고 MONGODB_URI 를 채운다
node --env-file=.env server.mjs
```

기동 로그가 이렇게 나오면 붙은 것이다.

```
G-Over World — http://localhost:8788
  세이브 저장소 : mongo (gover.saves)
```

이어서 다른 터미널에서:

```bash
node test/e2e.mjs
```

**21항목이 전부 `OK` 면 Atlas 설정은 끝났다.** 저장까지 실제로 오갔다는 뜻이다.
Atlas 화면의 **Browse Collections** 에서 `gover.saves` 에 문서가 생긴 것도
눈으로 확인할 수 있다.

| 증상 | 원인 |
|---|---|
| `Invalid scheme, expected connection string to start with "mongodb://"` | **`.env` 에 `MONGODB_URI=` 가 두 번 들어갔다** — 아래 참조 |
| `connect ECONNREFUSED` / `querySrv ENOTFOUND` | 문자열 오타, 클러스터 주소 잘못 |
| `bad auth : authentication failed` | 비밀번호 틀림, **또는 퍼센트 인코딩 누락** |
| `Server selection timed out` | Network Access 에 내 IP 가 없음 |
| `mongodb 드라이버가 없습니다` | `npm install` 을 안 했음 |

`.env` 가 이렇게 되기 쉽다. `MONGODB_URI=` 가 이미 있는 줄 뒤에 `MONGODB_URI=...`
통째를 붙여넣으면 **값이 키로 시작한다.**

```
MONGODB_URI=MONGODB_URI=mongodb+srv://...    ← 값이 "MONGODB_URI=mongodb+..." 가 된다
```

형식만 확인하고 싶으면(비밀번호는 찍지 않는다):

```bash
node -e "const l=require('fs').readFileSync('.env','utf8').split(/\r?\n/).find(x=>x.startsWith('MONGODB_URI='));const v=l.slice(12);console.log('스킴',v.startsWith('mongodb+srv://')||v.startsWith('mongodb://'),'| 길이',v.length)"
```

저장소만 따로 두들겨 보는 것도 된다.

```bash
node --env-file=.env -e "const {openStore}=await import('./src/store.mjs');const s=await openStore();console.log(s.kind,s.where);const p='0'.repeat(24);await s.put(p,{probe:1});console.log('쓰기/읽기',(await s.get(p)).probe===1?'OK':'실패');await s.del(p);await s.close()"
```

#### 6.2.6 환경변수 정리

| 변수 | 기본값 | 설명 |
|---|---|---|
| `MONGODB_URI` | (없음) | 없으면 `data/` 에 파일로 저장 |
| `MONGODB_DB` | `gover` | 데이터베이스 이름 |
| `MONGODB_COLLECTION` | `saves` | 컬렉션 이름 |
| `SAVE_DIR` | `./data` | 파일 저장소 경로 |
| `PORT` | `8788` | 서버 포트 |

문서 모양은 `{ _id: 플레이어코드, g: 세이브, updated, created }` 다.
**연결은 모듈 전역에 캐시한다** — 서버리스에서 요청마다 새로 연결하면
커넥션이 폭발한다 ([§11.24](#1124-서버리스에서-db-클라이언트는-모듈-전역에-캐시할-것)).

---

### 6.3 Vercel 배포

#### 6.3.1 구성

```
vercel.json
  buildCommand      node build.mjs        docs/index.html · docs/app.js 재생성
  installCommand    npm install --omit=dev  mongodb 만 설치(sharp 제외)
  outputDirectory   docs                  정적 자산
  functions         api/[...path].mjs     /api/* 전부를 받는다
```

`api/[...path].mjs` 는 **파일명 자체가 라우트다.** Vercel 의 catch-all 규칙이라
`rewrites` 를 따로 걸 필요가 없다. `includeFiles` 가 `src/roster.json` 을 함수
번들에 같이 넣는다 — 이게 빠지면 함수가 로스터를 못 읽어 500 이 난다.

#### 6.3.2 순서

1. 커밋한 것을 GitHub 에 올린다 (`git push`)
2. [vercel.com/new](https://vercel.com/new) → **Import Git Repository** → 저장소 선택
3. **Framework Preset: `Other`**
   Build / Output 설정은 **건드리지 않는다** — `vercel.json` 이 이미 지정한다
4. **Environment Variables** 를 펼쳐 `MONGODB_URI` 를 추가한다

   | Key | Value | Environments |
   |---|---|---|
   | `MONGODB_URI` | 6.2.4 에서 만든 문자열 | Production · Preview · Development 전부 |

   > **이걸 빠뜨리면 게임이 뜨긴 하는데 아무것도 안 된다.** Vercel 의 파일
   > 시스템은 읽기 전용이라 파일 저장소를 쓸 수 없다. 빠뜨렸을 때는 API 가
   > 그 사실을 한국어로 알려 준다.
5. **Deploy**. 첫 배포는 이미지 2488장(133MB) 때문에 몇 분 걸린다.

#### 6.3.3 배포 후 확인

```bash
# 1) API 가 살아 있는가 — 이게 제일 중요하다
curl -s https://<프로젝트>.vercel.app/api/state
# → {"pid":"...","g":null,"store":"mongo"}

# 2) 정적 자산과 캐시 헤더
curl -sI https://<프로젝트>.vercel.app/app.js | grep -i cache-control
curl -sI https://<프로젝트>.vercel.app/img/1001000150.webp | grep -i cache-control
# → public, max-age=31536000, immutable

# 3) 종단 점검 21항목을 배포본에 그대로 돌린다
BASE=https://<프로젝트>.vercel.app node test/e2e.mjs
```

**3번이 전부 `OK` 면 배포가 끝난 것이다.**

#### 6.3.4 안 될 때

| 증상 | 원인 / 조치 |
|---|---|
| `/api/state` 가 **404** | 함수가 안 잡혔다. `api/[...path].mjs` 가 저장소에 올라갔는지, `vercel.json` 의 `functions` 키 이름이 파일 경로와 정확히 같은지 확인 |
| **500** + `MONGODB_URI 가 설정되지 않았습니다` | 환경변수 누락. **추가한 뒤에는 반드시 재배포해야 한다** (Deployments → ⋯ → Redeploy). 이미 뜬 배포에는 소급 적용되지 않는다 |
| **500** + 그 외 | Vercel **Logs** 탭을 본다. `roster.json` 을 못 찾으면 `includeFiles` 문제, 인증 실패면 Atlas 문제 |
| 화면은 뜨는데 `서버 연결 실패` | `/api/state` 를 직접 열어 본다. 위 세 줄 중 하나로 갈린다 |
| Atlas 에서 `Server selection timed out` | Network Access 에 `0.0.0.0/0` 이 없다 |

이후 `git push` 할 때마다 자동 재배포되고, 브랜치마다 프리뷰 URL 이 따로 생긴다.
**프리뷰도 같은 `MONGODB_URI` 를 쓰면 세이브를 공유한다** — 나누고 싶으면
Preview 환경에만 다른 `MONGODB_DB` 를 지정한다.

#### 6.3.5 검증 범위

전 경로를 실제로 돌려 확인했다.

| 경로 | 상태 |
|---|---|
| 파일 저장소 (`data/`) | 실측 — e2e 21항목 |
| MongoDB 연결 실패 처리 | 실측 — 오류 한 줄 보고 후 종료 |
| MongoDB Atlas M0 실연결 | 실측 — 쓰기·읽기·삭제 + e2e 21항목 (`store=mongo`) |
| **Vercel 배포** | **실측** — 아래 |

Vercel 배포본(`g-over-world.vercel.app`)에서 확인한 것:

* `api/[...path].mjs` catch-all 라우팅이 `rewrites` 없이 잡힌다
* `MONGODB_URI` 환경변수가 함수에 주입되어 `/api/state` 가 `store:"mongo"` 응답
* `includeFiles` 로 들어간 `src/roster.json` 을 함수가 읽는다
* 캐시 헤더가 의도대로 — 이미지 `max-age=31536000, immutable`,
  `app.js`·`index.html` `max-age=0, must-revalidate`
* **종단 점검 21항목 전부 통과** (`BASE=https://... node test/e2e.mjs`)
* 브라우저에서 로스터 1244기 로드, 콘솔 오류 0, 깨진 이미지 0

그래도 **6.2.5(로컬 Mongo 검증)를 먼저 하라**는 권고는 유지한다. 배포에서
막혔을 때 원인이 Atlas 인지 Vercel 인지 즉시 갈리기 때문이다.

---

### 6.3-b 다른 곳에 올리기 (Render · Fly.io · VPS)

서버리스가 아니라 상시 프로세스라면 **MongoDB 없이 파일 저장소로도 된다.**

```bash
git clone <저장소> && cd G_Over_World
npm install
node build.mjs
PORT=8080 node server.mjs
```

* 시작 명령 : `npm start`
* 필요한 것 : Node 20 이상, 디스크 약 150MB
* 세이브 : `data/` — **컨테이너를 재생성하면 날아가므로** 볼륨을 붙이거나
  `MONGODB_URI` 를 쓴다
* 리버스 프록시(nginx 등) 뒤에 둘 거면 `/img` `/th` 는 프록시가 직접
  서빙하게 하는 편이 낫다 (133MB 를 Node 가 계속 읽을 이유가 없다)

---

### 6.4 GitHub Pages 는 더 이상 쓸 수 없다

Pages 는 정적 파일만 서빙한다. 서버가 필요해진 이상 선택지에서 빠진다.

| 배포처 | 가능 | 필요한 것 |
|---|---|---|
| GitHub Pages | ✕ | — (정적 전용) |
| Vercel | ○ | `MONGODB_URI` |
| Render · Fly.io · VPS | ○ | `npm start`, 세이브는 파일 또는 Mongo |
| 로컬 | ○ | 없음 |

**정적 배포로 돌아가고 싶다면** v4 태그 시점의 `engine.js` + `ui.js` 를 되살리면
된다. 규칙은 `rules.mjs` 에 그대로 있으므로, `actions.mjs` 를 브라우저로 옮기고
저장을 `localStorage` 로 되돌리는 정도의 작업이다.

#### 대역폭

이미지가 여전히 전체 용량의 97% 다. 한 명이 전 기체를 열람해도 134MB 가
상한이라 5인이 매일 놀아도 월 1~4GB — Vercel Hobby 100GB 의 1~4% 다.
`vercel.json` 이 이미지에 1년 불변 캐시를 걸므로 재방문 비용은 거의 0 이다.

| 1인 1세션 (캐시 없는 첫 접속) | 사용량 |
|---|---|
| 가볍게 — 전투 20회 | 8MB |
| 보통 — 전투 30회 + 도감 한 바퀴 | 24MB |
| 이론상 최대 — 1244기 전부 열람 | 134MB |

### 6.5 세이브 · 플레이어 코드

세이브는 서버에 있고, 브라우저는 `gow_pid` 쿠키(24자리 16진수)로 식별된다.

* **다른 기기에서 이어하기** — 화면 우상단 `【이어하기 코드】` 에 뜨는 코드를
  그쪽 브라우저의 같은 화면에 넣는다.
* **기록 말소** — `【기록말소】` (확인 창 있음). 서버에서 지운다.
* 코드는 `crypto.randomBytes(12)` 로 만든다. **추측 가능하면 남의 세이브가
  열리므로** 반드시 CSPRNG 를 쓸 것.
* 코드를 아는 사람은 그 기록을 열 수 있다. 4~5인이 함께 노는 용도로는
  충분하지만, 비밀번호가 아니라는 점은 화면에도 적어 두었다.

---

## 7. 게임 설계 명세

### 7.1 세이브 상태 (`g`)

**서버가 소유한다.** MongoDB 문서 또는 `data/<플레이어코드>.json` 에 통째로
들어가고, 브라우저에는 그리기 위한 사본만 내려간다. 이미지·제원은 저장하지
않고 **`id` 만** 들고 있다가 `UMAP[id]` 로 참조한다. 실측 크기 **0.7KB**.

```js
{
  name,                         // 파일럿명 (소속 개념은 없다)
  lv, exp, pt, cash,            // pt = 미배분 능력 포인트
  ap, apMax, day,
  mor0,                         // 전투 개시 기력 (100 + floor(정신/25), 이벤트로 상승)
  st: { sho, mel, def, rea, awk, spi },   // 사격·격투·수비·반응·각성·정신, 각 상한 999
  sk: { 스킬키: 레벨, ... },    // 습득한 파일럿 스킬 (Lv1~10)
  eq: [ '스킬키', ... ],        // 장착한 스킬 (최대 PSK_SLOT = 3)
  parts: { sp, ai, gr, se, uw },// 상점에서 사 둔 파츠 재고
  orders: [ { id, due, price } ],// 발주 중인 기체 — due 일에 격납고로 들어온다
  kills, sorties, wins, losses, downs, fame,
  garage: [{
    id,                         // UNITS 의 id
    hp,                         // 현재 HP (출격 사이 유지)
    ulv, uexp, upt,             // 기체 레벨 / EXP / 미배분 강화 포인트
    inv: { hp, en, atk, def, mob, sct },  // 기체 레벨로 투자한 포인트 수
    mod: { hp, en, def, mob, atk, sct },  // 각 0~10단
    wl: [ 1, 1, ... ],          // 무장별 레벨, 기체의 무장 수만큼
    pt: [ 'sp', 'uw' ]          // 장착한 파츠 (최대 PART_SLOT = 3)
  }],
  cur,                          // garage 인덱스 = 탑승기
  records: [ {day, m, t, r, kills, pay, exp, hp} ],   // t = 전장 키. 최근 40건
  flags: { bossDown }
}
```

로드할 때 `UMAP[id]` 로 실재 여부를 검증하고, 없는 기체는 걸러낸다.
**로스터를 바꾸면 기존 세이브의 일부 기체가 사라질 수 있다.**

세이브 구조가 자라도 옛 기록이 깨지지 않도록 `load()` 가 누락 필드를 채운다
(`pt` · `sk` · `eq` · `parts` · `orders` · `inv` · `ulv` …). 이 보정은 이제
`api.mjs` 가 세이브를 꺼낼 때가 아니라 **`rules.mjs` 의 마이그레이션 자리**에
있어야 한다 — 클라이언트가 세이브를 만들지 않기 때문이다.

### 7.2 캐릭터 능력치

여섯 항목뿐이고 **상한은 999**, 레벨 상한도 999다.

| 키 | 이름 | 작용 |
|---|---|---|
| `sho` | 사격 | 사격 무장의 명중과 화력 |
| `mel` | 격투 | 격투 무장의 명중과 화력 |
| `def` | 수비 | 피탄 시 장갑 환산치 (구 '방어'와 같은 능력치, 이름만 바뀜) |
| `rea` | 반응 | 회피 · 거리 싸움 · 명중 |
| `awk` | 각성 | 각성 무장의 명중·회피·사거리, 그리고 미래 예측 |
| `spi` | 정신 | 기력 · 패널티 경감 · 크리티컬 · 저력 |

구 '기량'은 폐지하고 **각성**으로 대체했다. 크리티컬 필요마진은 기량 대신
사격/격투와 **정신**에서 뽑는다([8.6](#86-크리티컬)).

> **적 발견은 색적의 영역이다.** 초기 설계에서는 반응이 `spotBon`/`hideBon` 에도
> 들어가 있었지만, 색적을 신설한 이상 같은 일을 두 능력치가 하면 안 된다.
> 반응은 회피·거리 싸움·명중에만 남겼다.

**포인트 배분 (#4 · #6)**

```
생성 시     START_PT = 120 을 여섯 항목에 자유 분배 (잔여 0이어야 등록 가능)
레벨업 1회  LVUP_PT  = 5   포인트 지급 → 【훈련】에서 배분, 1 소모 = 1 상승
```

`LVUP_PT = 5` 는 의도적으로 모자라게 잡은 값이다.

```
2~999 레벨 = 998회 × 5 = 4,990 + 초기 120 = 5,110
6능력치 만렙 합계     = 6 × 999          = 5,994
```

**레벨업만으로는 전 능력치 999 를 채울 수 없다.** 모자란 884 이상은 훈련과
일일 이벤트로 메워야 하고, 훈련은 400 이상에서 성공률 1% 고정이므로
"무엇을 포기할지"가 육성의 실질적인 선택이 된다.

### 7.3 각성과 미래 예측 (#1)

**각성 무장** = 속성에 `특수` 가 들어간 무장 (`wIsAwk`). 전 로스터 3,259종 중 453종.

| 조건 | 효과 |
|---|---|
| 각성 0 | **각성 무장을 아예 쓸 수 없다.** 목록에 '사용 불가' 로 표시 |
| 각성 ≥ 1 | 각성 무장 사용 가능 |
| 각성 값 | 각성 무장 명중 `+floor(각성/100)`, 피격 회피 `+floor(각성/100)` |
| 각성 값 | 각성 무장 최대 사거리 `+floor(각성/100)`, **상한 `AWK_RANGE_CAP = 13`** |
| 각성 ≥ 800 | 매 판정 **5%** 로 미래 예측 발동 |

미래 예측은 판정 직전에 공격측·방어측 각각 굴린다(공격측 우선).

```
공격 턴에 발동 → 「거기냣!」  절대 명중 (크리티컬은 마진으로 따로 판정)
방어 턴에 발동 → 「보인다!」  절대 회피
```

발동 기준치는 **그 턴의 실효 각성치**로 본다.

```js
const awkOf = u => clamp((u.st.awk|0) + (u.awkTmp|0), 0, STAT_MAX);
```

`awkTmp` 는 장비·파츠·스킬·전투 중 효과가 얹는 일시 보정분이다. 지금은 늘 0이지만,
여기에 값을 넣기만 하면 "그 턴만 각성 800 을 넘겨 미래 예측이 뜨는" 연출이
별도 코드 없이 성립한다. SEED 스킬이 정확히 이 자리를 쓴다([7.11](#711-파일럿-스킬-15)).

### 7.4 전장과 지형 적성 (#13~#16)

전장 5종: **우주 `sp` / 공중 `ai` / 지상 `gr` / 수상 `se` / 수중 `uw`**.
적성은 GGen API 의 `terrain[].level` 을 그대로 옮긴 것이다.

| API level | 심볼 | 게임 값 | 효과 |
|---|---|---|---|
| 3 | ● | 2 (`○`) | 제원 **100%** |
| 2 | ▲ | 1 (`△`) | **공격·기동 85%** |
| 1 | - | 0 (`×`) | **출격 불가** |

적 편성도 같은 규칙을 탄다. `buildFoes(ms, ter)` 는 그 전장에 적성이 있는
기체만 뽑고, 적성 △ 인 적기도 똑같이 85% 로 깎여 들어간다.

로스터의 적성 분포 — **전장 선택이 실제로 난이도를 바꾼다**:

| 전장 | × | △ | ○ |
|---|---|---|---|
| 우주 | 152 | 31 | 1,061 |
| 공중 | 885 | 40 | 319 |
| 지상 | 106 | 36 | 1,102 |
| 수상 | 1,200 | 0 | 44 |
| 수중 | 173 | 867 | 204 |

**수상은 ○ 가 44기(UR 1기)뿐이라 가장 척박하다.** 원 데이터가 그렇다.
반대로 수중은 867기가 △ 라 양쪽 다 85% 로 깎여 교전이 느슨해진다.

**지형 파츠** — 기체당 슬롯 `PART_SLOT = 3`.

```
구입   【상점】에서 개당 PART_PRICE = 220,000C — 창고(g.parts)에 쌓인다
장착   【개조】에서 창고에서 꺼내 단다. 슬롯 3칸을 지형 파츠와 (추후 추가될)
       다른 파츠가 함께 쓴다
효과   파츠 1개 = 해당 지형 적성 1단계 상승 (× → △ → ○)
       같은 파츠 2개 = × 에서 ○ 까지 (슬롯 두 칸 소모)
탈거   무상. 창고로 되돌아간다
```

가격을 기체 시세가 아니라 **정액**으로 둔 이유는, 파츠가 이제 특정 기체가 아니라
창고에 속하기 때문이다. 살 때는 어느 기체에 달지 정해져 있지 않다.

이걸로 "수상 적성이 없어서 출격 자체가 막히는" 상황을 자금으로 풀 수 있다.

### 7.5 화면 10종 + 다음 날

| 커맨드 | 뷰 함수 | AP | 하는 일 |
|---|---|---|---|
| 상황실 | `viewMain` | – | 일자·자금·경고·임무 일람·최근 전투 |
| 훈련 | `viewTrain` | 1 | 능력 포인트 배분 + 능력치 훈련 + **파일럿 스킬** |
| 출격 | `viewSortie` | 1~4 | **전장** → 전법 → 임무 |
| 정비 | `viewRepair` | 1 | HP 완전 회복, 손상량 비례 과금 |
| 개조 | `viewMod` | 0 | **기체 레벨 포인트** + 기체 6항목 + 파츠 장착 + 무장 레벨 |
| 상점 | `viewShop` | 0 | **기체 발주** + 파츠 구입 |
| 암시장 | `viewMarket` | 0 | 매일 갱신되는 1~10기 매물 |
| 격납고 | `viewHangar` | 0 | 탑승기 변경 / 매각 |
| 도감 | `viewBook` | 0 | 1,244기 작품 필터 + 이름·형식번호 검색 |
| 기록 | `viewLog` | 0 | 통계 + 출격 이력 |
| **다음 날** | `nextDay` | – | 유지비 차감, AP 회복, 랜덤 이벤트, **암시장 초기화** |

`S.view` 문자열로 분기하고 `renderAll()` = `renderLeft()` + `renderMain()` + `renderCmd()`.

> **구 '개발' 화면은 삭제했다.** 기체 획득 경로는 두 개다 —
> **암시장**(싸고 즉시, 매물은 운) 과 **상점 발주**(비싸고 느리지만 지정 가능).

### 7.6 파생 수치

```js
/* 기체 레벨 투자분을 소재값에 더하고, 거기에 개조 배수를 곱한다 */
uBase(v, k)   = UNITS[v.id][k] + v.inv[k] * UINV_STEP[k]     // HP 는 1pt = +100
uStat(v) = {                                   // 전부 UCAP 으로 잘린다
  hpMax : uBase(v,'hp')  * (1 + mod.hp  * 0.05),   // 상한 999,999
  enMax : uBase(v,'en')  * (1 + mod.en  * 0.05),   // 상한 999
  def   : uBase(v,'def') * (1 + mod.def * 0.05),   // 상한 9,999
  mob   : uBase(v,'mob') * (1 + mod.mob * 0.05),   // 상한 9,999
  atk   : uBase(v,'atk') * (1 + mod.atk * 0.05),   // 상한 9,999
  sct   : uBase(v,'sct') * (1 + mod.sct * 0.05)    // 상한 9,999
}
adaptOf(v, k) = clamp(B.tr[k] + 장착한 k 파츠 수, 0, 2)
wpowOf(v, i)  = UMAP[v.id].w[i].pw[ wl[i] - 1 ]

/* 파일럿 스킬을 반영한 실효 능력치 — 화면과 전투가 같은 값을 쓴다 */
effStats(B) = {
  st  : clamp(g.st[k] * (1 + eff.st[k] − eff.pst[k] * pen), 0, 999),
  pen : penMulOf(g.st.spi, eff)
}
```

개조 1단 = 기본 제원의 5%, 최대 10단 = **+50%**. 소속 계수는 없다.
개조 배수가 **기체 레벨 투자분까지 곱한다**는 점에 주의 — 투자와 개조는 곱셈으로 겹친다.

### 7.7 경제

기체 가격은 **등급이 아니라 제원 총합**에서 나온다(#18). 등급 표기를 지운
이상 값도 등급에서 뽑으면 안 되기 때문이다.

```js
uPow(B)  = B.hp*0.55 + (B.atk*3 + B.def*2.2 + B.mob*2.2 + B.sct*1.6)*22 + B.en*140
시세     = round(pow(uPow/1000, 1.6) * 90 * (대형기면 1.1) / 100) * 100
요구 Lv  = clamp(round((uPow − 100000) / 12000), 1, 40)
```

| 항목 | 공식 |
|---|---|
| 암시장가 | `시세 × (0.88 ~ 1.12)` — 날짜 시드로 고정된 흥정폭 |
| 발주가 | `시세 × 1.15` (선불) · 취소 시 50% 환불 |
| 발주 기간 | `clamp(round(uPow / 26000), 3, 20)` 일 — 실측 5~14일 |
| 발주 계급 | 로스터 성능 **백분위를 8등분**해 RANKS 에 대응 |
| 파츠 | `PART_PRICE = 220,000C` 정액 |
| 스킬 습득·강화 | `round(80000 × (현재Lv+1)^1.6 / 1000) × 1000` |
| 매각가 | `시세 × 0.55` (개조·파츠 투자분 환불 없음) |
| 개조 비용 | `round(시세 × 0.045 × 1.34^현재단수 / 100) × 100` |
| 무장 강화 | `round(시세 × 0.05 × 현재레벨 / 100) × 100` |
| 지형 파츠 | `round(시세 × 0.10 / 1000) × 1000` |
| 정비 | `ceil((hpMax − hp) × 0.16 / 10) × 10` |
| 부대 유지비 | `1500 + 레벨 × 400` (다음 날마다) |
| 초기 자금 | `START_CASH = 80,000C` |

실제 값 — 최저 209,100C(요구 Lv2) · 중앙값 401,300C(Lv8) · 최고 1,127,200C(Lv22).

### 7.7-b 상점 발주 (#5 · #6)

암시장이 **싸지만 운**이라면, 상점은 **비싸지만 지정**이다.

```
발주   원하는 기체를 골라 선불 → orders 에 { id, due, price } 로 쌓인다
인도   nextDay() 에서 g.day >= due 인 주문을 격납고로 옮긴다
       이미 같은 기체를 갖고 있으면 인도 대신 50% 환불
취소   언제든 가능하나 50%만 돌아온다
```

**계급 제한**은 고정 금액 구간으로 자르면 안 된다. 처음에는
`floor((uPow − 110000) / 30000)` 로 잘랐더니 대령 1기 · 중위 305기가 나왔다.
로스터 전체 성능의 **백분위**를 8등분해 155기씩 고르게 배분한다.

```js
const POW_SORTED = UNITS.map(uPow).sort((a, b) => a - b);
const rankReqOf = B => clamp(Math.floor(powPct(B) * RANKS.length), 0, RANKS.length - 1);
```

| 계급 | 훈련병 | 소위 | 중위 | 대위 | 소령 | 중령 | 대령 | 준장 |
|---|---|---|---|---|---|---|---|---|
| 필요 격추 | 0 | 3 | 10 | 24 | 45 | 75 | 115 | 170 |
| 발주 가능 기체 | 156 | 157 | 154 | 155 | 156 | 155 | 156 | 155 |

### 7.8 암시장 (#18)

```js
marketOf(day)   // day 를 시드로 한 mulberry32 PRNG
  → 1~10기, 중복 없음, 무장 없는 기체 제외
```

**날짜만 알면 같은 매물이 나온다.** 하루 안에 화면을 몇 번 다시 그려도 목록이
흔들리지 않고, 세이브에 재고를 따로 담을 필요도 없다. 60일 평균 5.7기.

레벨별로 실제 살 수 있는 매물이 있는 날 (60일 기준):

| 레벨 | 매물 있는 날 | 일 평균 구입가능 기수 |
|---|---|---|
| Lv1 | 0 / 60 | 0.0 |
| Lv3 | 26 / 60 | 0.5 |
| Lv5 | 49 / 60 | 1.6 |
| Lv10 | 56 / 60 | 3.5 |
| Lv20 | 60 / 60 | 5.7 |

Lv1 에 아무것도 못 사는 것은 의도다(최저 요구 Lv 가 2). 초계 임무 한 번이
약 33,000C·40EXP 를 주므로 하루면 Lv3 대에 올라선다.

### 7.9 성장

```js
expNeed(lv) = round(100 × lv^1.45)     // LV_MAX = 999
```

레벨업 시: **능력 포인트 +5**, 자금 +4,000C, **4레벨마다 AP 상한 +1**
(시작 10 — `AP_BASE` 상수). 임무·훈련 양쪽에서 EXP가 들어온다.

계급은 격추수로만 결정: 훈련병 0 / 소위 3 / 중위 10 / 대위 24 / 소령 45 /
중령 75 / 대령 115 / 준장 170.

### 7.10 훈련 판정 (#7)

**능력치 상한은 없다(999 까지).** 대신 구간별로 성공 확률이 계단식으로 떨어지고,
**400 이상은 1% 고정**이다. 2d6 이 아니라 d100 직접 판정이다.

| 현재 능력치 | 성공률 | 성공 시 상승 |
|---|---|---|
| 0 ~ 49 | 90% | +4 |
| 50 ~ 99 | 70% | +3 |
| 100 ~ 149 | 50% | +3 |
| 150 ~ 199 | 35% | +2 |
| 200 ~ 249 | 22% | +2 |
| 250 ~ 299 | 14% | +1 |
| 300 ~ 349 | 8% | +1 |
| 350 ~ 399 | 4% | +1 |
| **400 이상** | **1%** | +1 |

```
EXP = 18 + 상승치 × 12   (실패해도 18)
AP  = TRAIN_AP = 1
```

400 이후 기댓값은 훈련 100회당 +1 이다. 여기서부터는 훈련이 아니라 레벨업
포인트와 일일 이벤트가 주 성장원이 된다.

### 7.11 파일럿 스킬 (#15)

훈련 화면에서 **자금으로 습득하고 레벨을 올린다**(Lv1~10). 장착은 최대
`PSK_SLOT = 3` 개. `g:'origin'` 이 붙은 일곱 개는 **출신 계통**이라 서로
동시에 장착할 수 없다 — 뉴타입 · 강화인간 · 올드타입 · SEED · 코디네이터 ·
이노베이터 · 이노베이드.

| 스킬 | Lv1 → Lv10 | 비고 |
|---|---|---|
| 뉴타입 〔출신〕 | 각성·반응 +2% → +20% | |
| 강화인간 〔출신〕 | 각성·반응·수비 +2% → +20% / 정신 −1% → −10% | 하락분은 패널티라 정신이 깎아 준다 |
| 카리스마 | 적 반응 −2% → −20% | 적 편성 생성 시에 적용 |
| 사격 전문가 | 사격 무장 최대 사거리 **+1(고정)** / 사격 EN −1% → −10% | |
| 격투 전문가 | 격투 무장 최대 사거리 **+1(고정)** / 격투 EN −1% → −10% | |
| 포커페이스 | 모든 패널티 −5% → −50% | 상한 50% |
| 올드타입 〔출신〕 | 전 능력치 +1% → +10% · **각성 0 고정** | 훈련·포인트 배분도 잠긴다 |
| 철벽 | 수비 +2% → +20% | |
| 광전사 | 사격·격투·반응 +3% → +30% / 수비 −2% → −20% / 각성 −1% → −10% / **받는 패널티 +1% → +10%** | |
| 사신 | 시야에서 사라진 뒤 첫 공격이 10% → 100% 로 크리티컬 확정 | |
| 오퍼레이션 메테오 | 개시 10턴간 기체 공격·방어·기동 +1% → +10% | |
| SEED 〔출신〕 | 매 턴 5% → 50% 로 발동. 발동 시 **전투 종료까지** 각성·반응 +20%(고정) | |
| 코디네이터 〔출신〕 | 사격·격투·수비·반응 +1% → +10% | |
| 건담 마이스터 | 기체명에 '건담'이 들어가면 정신 +2% → +20% | |
| 이노베이터 〔출신〕 | 각성·반응·정신 +1.5% → +15% | |
| 이노베이드 〔출신〕 | 사격·격투·각성·반응 +1% → +10% | |
| 역전의 용사 | **레벨 없음.** 파일럿 Lv500 에서 사격·격투·수비·반응 +10%, Lv900 이상 +20% | 그 사이는 선형 |

**효과 집계.** `skillEffect(ctx)` 가 효과 객체 하나를 채우고, `effStats(B)` 가
그것을 실제 능력치에 얹는다. 화면과 전투가 같은 함수를 쓰므로 표시와 계산이
어긋날 수 없다.

```js
최종 능력치 = clamp(g.st[k] * (1 + x.st[k] − x.pst[k] * penMul), 0, 999)
```

`x.st` 는 순수 상승률, `x.pst` 는 **하락률**이다. 둘을 나눠 둔 이유는
강화인간의 정신 −10% 나 광전사의 수비 −20% 가 "패널티"이기 때문이다.
정신과 포커페이스는 이것도 깎아 준다([7.13](#713-정신--패널티와-저력-14)).

**비용.** `round(80000 × (현재Lv+1)^1.6 / 1000) × 1000`

| Lv1 | Lv2 | Lv5 | Lv8 | Lv10 | 한 스킬 Lv10 누적 | 3개 |
|---|---|---|---|---|---|---|
| 80,000 | 243,000 | 1,051,000 | 2,229,000 | 3,185,000 | 13,884,000 | **41,652,000C** |

**'건담' 판정에 주의.** 로스터 이름은 영문(`[SD] Gundam (EX)`)이라 한글
`'건담'` 만 찾으면 아무것도 걸리지 않는다. 양쪽을 다 본다.

```js
const isGundamName = B => /건담|gundam/i.test(B.nm || '') || /건담|gundam/i.test(B.mdl || '');
```

**추가 스킬을 넣으려면** `PSKILL` 에 항목 하나를 더하면 된다. 상점·장착 UI·
효과 집계가 전부 이 배열을 순회하므로 다른 곳은 손댈 필요가 없다.

### 7.12 기체 레벨 (#1~#4)

출격한 탑승기는 파일럿과 **같은 EXP** 를 받아 따로 성장한다.

```js
uExpNeed(lv) = round(80 × lv^1.32)     // ULV_MAX = 9999
레벨업 1회   → 강화 포인트 ULVUP_PT = 4
포인트 1점   → HP +100 / EN·공격·방어·기동·색적 +1
```

`ULVUP_PT = 4` 는 **의도적으로 모자라게 잡은 값**이다. 전 항목을 상한까지
채우는 데 드는 포인트를 로스터 전체에 대해 계산하면 최소가 44,565 다
(가장 강한 Full Armor ZZ Gundam (EX) 기준). 레벨업으로 얻는 총량은:

| pt/레벨 | 9998레벨분 | 판정 |
|---|---|---|
| 3 | 29,994 | 도달 불가 |
| **4** | **39,992** | **도달 불가 — 채택** |
| 5 | 49,990 | 도달 가능 → 부적합 |

즉 **기체 레벨업만으로는 전 능력치 최대에 도달할 수 없다.** 나머지는 개조
10단(+50%)과 파츠로 메워야 하고, 결국 "어느 항목을 포기할지"가 남는다.

투자분은 개조 배수 **안쪽**에 들어간다. `uBase()` 로 소재값을 만든 뒤 개조
배수를 곱하므로, 레벨 투자와 개조는 곱셈으로 겹친다.

### 7.13 정신 — 패널티와 저력 (#14)

정신은 세 가지 일을 한다.

**1) 패널티 경감.** 파일럿·기체에 붙는 **모든 음(−) 보정**을 깎는다.

```js
penMul = clamp(1 − (정신/999) × 0.30 − 포커페이스감소 + 광전사증가, 0.10, 1.60)
```

`penMul` 이 곱해지는 대상 — 이 목록이 곧 "패널티"의 정의다.

| 대상 | 원래 값 |
|---|---|
| 지형 적성 △ | 공격·기동 −15% |
| 전법의 음 보정 | 맹공 명중 −2 · 받는 피해 +28% / 신중 화력 −10% / 회피 화력 −18% |
| 사거리 밖(`desperate`) | 회피 −2 · 받는 피해 +12% |
| 대형기 | 회피 −1 |
| 스킬 자체 하락분 | 강화인간 정신 −10% · 광전사 수비 −20% / 각성 −10% |

전법의 **양(+) 보정은 건드리지 않는다.** 신중의 명중 +2 는 그대로 두고
화력 −10% 만 완화한다. 안 그러면 정신이 전법 선택 자체를 무의미하게 만든다.

**2) 크리티컬.** 필요 마진을 `floor(정신 / 150)` 만큼 낮춘다(최대 −6).

**3) 저력.** HP 가 30% 이하로 떨어지면 **매 라운드** 판정을 굴린다.

```js
gutsP(정신) = 0.001 + (정신 − 1) / 998 × 0.998     // 정신 1 → 0.1% · 999 → 99.9%
발동 시 GUTS_TURN = 10 턴간 명중률 ×1.30 · 회피율 ×1.30 · 방어력 ×1.30
```

방어력은 그대로 곱하면 되지만, **명중률·회피율 30%** 는 2d6 대항판정에
그대로 곱할 수 없다. 확률에 배수를 걸고 **그 확률에 가장 가까운 우열값**으로
되돌린다. 주사위 굴림 서사를 그대로 두면서 수치를 지킬 수 있다.

```js
function advForP(t) {           // OPP 표에서 목표 확률에 가장 가까운 우열
  let best = -14, bd = 9;
  for (let i = -14; i <= 14; i++) { const d = Math.abs(OPP[i] - t); if (d < bd) { bd = d; best = i; } }
  return best;
}
const advScale = (adv, mul) => advForP(clamp(oppP(adv) * mul, 0.02, 0.98));
```

저력 중에는 우열 상한이 `±HIT_CLAMP` 가 아니라 `±8` 로 열린다. 역전 장치이므로
평소의 교착 방지 상한을 넘어서게 두는 것이 맞다. **적기도 같은 규칙을 탄다** —
지휘기가 마지막에 버티는 장면이 여기서 나온다.

### 7.14 일일 이벤트

가중 추첨(총 가중치 100). 자금 획득/손실, HP 손상/완전정비, 능력치 +1,
AP 완전회복, 초기 기력 +3(상한 118), 명성 +3, 무변화.

---

## 8. 전투 규칙 전문

전투는 **완전 자동**이다. 플레이어의 개입 지점은 출격 전 **전장 선택 + 전법 선택** 둘뿐.

### 8.1 라운드 구조

최대 `ROUND_CAP = 24` 라운드. 한 라운드는 네 단계로 진행한다.

```
1) EN 회복 ─ 전 유닛 EN += ceil(enMax × EN_REGEN)      // 10%
   지속 효과 감쇠 (저력 잔여 턴 −1, 메테오 10턴 만료)
   SEED 판정 (매 턴) · 저력 판정 (HP 30% 이하인 유닛 전원)
2) 거리 싸움 ─ 교전 거리(1~20) 를 재조정
3) 색적 ─ 시야에서 사라지거나 다시 잡아낸다
4) 행동 ─ 생존 유닛이 이니셔티브 순으로 1회씩
```

**행동 순서(이니셔티브):**

```
MOB + 색적 × 0.6 + (선제 무장 보유 시 900) + d6 × 320
```

내림차순, 매 라운드 다시 굴린다. 단 **1라운드만은 전투 개시 색적 판정에서
이긴 쪽이 먼저** 움직인다(선공권).

**표적 선택:** 플레이어는 HP 비율이 낮은 적을 우선하되 `±0.225` 난수로 흔든다.
**숨은(로스트) 상대는 후보에서 빠진다.**

**무장 선택:** `명중확률 × 예상피해` 가 최대인 것을 자동으로 고른다.
EN 부족·탄약 소진·**현재 거리 밖**·**각성 0인데 각성 무장** 은 후보에서 빠진다.

**화면 표시:** `paintDuel()` 이 공격기와 피격기의 초상화를 좌우로 크게 띄우고
가운데에 `교전 → HIT/CRITICAL/MISS + 피해량`, 미래 예측 시 대사를 찍는다.
`paintBoard()` 가 양쪽 전 유닛의 **HP·EN 게이지**를 갱신하고, 상단 `#dist` 가
전장명과 교전 거리 막대를 그린다. 넷 다 `S.duel`/`S.board`/`S.dist`/`S.blog` 에
보관되므로 전투가 끝나고 결과 화면을 다시 그려도 기록이 남는다
([11.5](#115-전투-종료-시-로그가-사라진다) 참조).

### 8.2 거리 (#12)

무장 사거리보다 **넓은 개념**이다. `DIST_MIN = 1` ~ `DIST_MAX = 20`,
전투 진입 시 `rint(1, 20)` 으로 무작위 지정.

원 데이터의 사거리는 1~5뿐이라 그대로 쓰면 거리 6 이상에서 아무도 못 쏜다.
`RANGE_MUL = 2` 로 거리축에 편다.

```js
mn = max(1, w.mn * 2 − 1)
mx = max(mn, w.mx * 2)
mx += 격투 무장이면 rangeM, 사격 무장이면 rangeS      // 전문가 스킬 +1 (고정)
각성 무장이면 mx = min(13, mx + floor(각성/100))
```

결과적으로 격투는 1~2, 사격은 최대 6~10, 각성 무장은 최대 13까지 닿는다.
사격·격투 전문가는 **레벨과 무관하게 +1 고정**이라 초반 1레벨에서 가장 값지다.

**거리 조정** — 매 라운드 양쪽이 희망 방향을 정한다.

| 상황 | 희망 |
|---|---|
| 나는 닿고 상대는 못 닿는다 | **0 (유지)** — 이 사거리를 최대한 지킨다 |
| 내 사거리 밖이다 | 자기 사거리 구간 쪽으로 (−1 접근 / +1 이탈) |
| 둘 다 닿는다 | 자기 구간 안이면 유지 |

```
방향이 같으면   step = 방향 × (1 + floor((내속도 + 적속도) / 4))   // 서로 달려든다
방향이 다르면   2d6 + 속도 대항판정, 이긴 쪽 방향으로
                step = 방향 × (1 + floor(|차이| / 4))
속도 mvSpd(u) = clamp(2 + floor(MOB/300) + floor(반응/120), 2, 9)
```

**공격 수단이 없는 쪽은 수비를 희생한다(`desperate`).**

```
거리 판정 굴림 +2   (필사적으로 사거리를 잡으러 간다)
회피 −2 · 받는 피해 +12%
```

### 8.3 색적 (#11)

`sct` 는 원 데이터에 없는 능력치다. 수집 단계에서 파생시킨다.

```js
sct = min(9999, round(MOB*0.55 + EN*0.9 + MOV*30))   // fetch-roster.mjs
```

| 국면 | 판정 |
|---|---|
| 전투 개시 | `2d6 + spotBon` 대항 → 이긴 쪽이 **선공권** |
| 시야 이탈 | `clamp(0.08 + (hideBon − 상대 spotBon) × 0.03, 0.02, 0.38)` |
| 재포착 | `2d6 + 탐색측 spotBon` vs `2d6 + 은신측 hideBon` |
| 명중 보정 | `clamp(floor((공격 색적 − 방어 색적) / 250), −3, +3)` |

```js
spotBon(u) = floor(색적 / 60)     // 발견은 온전히 색적의 몫
hideBon(u) = floor(MOB / 130)     // 숨는 쪽은 기체 기동
```

> 초기에는 두 식에 반응이 함께 들어가 있었다. 색적을 신설한 이상 같은 일을
> 두 능력치가 하면 능력치 설계가 흐려진다. 반응을 빼면서 나눗수를
> 100 → 60 · 220 → 130 으로 낮춰 발견 판정의 진폭을 유지했다.

**숨은 유닛은 때릴 수 없다.** 다만 **공격하면 그 즉시 위치가 드러난다**
(`A.hidden = false; A.exposed = true`) — 그리고 `exposed` 인 라운드에는
다시 숨을 수 없다. 이게 없으면 기동 높은 기체가 영구히 숨어 전투가 성립하지 않는다.

### 8.4 EN (#23)

```
매 라운드 시작 : EN += ceil(enMax × 0.10)
무장 사용      : EN −= w.en
EN 0           : 어떤 무장도 사용 불가 (usable() 이 즉시 false)
```

EN 게이지는 전투판·교전 표시부·좌측 패널 모두에 **주황색**으로 뜬다(#24).
고연비 무장만 든 기체는 실제로 중간에 손이 묶인다.

### 8.5 명중 판정

```
공격측 = floor(사격|격투 / 6) + 기력보정 + 전법보정
       + round((무장명중 − 100)/8) + 색적차 + (각성무장이면 floor(각성/100))

방어측 = floor(반응 / 6) + 기력보정 + floor(MOB / 350) + 전법회피
       + (각성무장이면 floor(각성/100)) + (대형기면 −1) + (desperate 면 −2)

우열 = clamp(공격측 − 방어측, −6, +6)
저력 중이면 advScale 로 ×1.30 / ÷1.30 한 뒤 ±8 로 다시 clamp
명중 = (2d6 + 우열) ≥ 2d6
```

`대형기 −1` 과 `desperate −2` 에는 `penMul` 이 곱해진다 — 정신·포커페이스가
이 패널티들을 실제로 깎는다([7.13](#713-정신--패널티와-저력-14)).

`STAT_HIT_K = 6` 과 `MOB_EVA_K = 350` 의 **비율**이 이 식의 전부다.
능력치가 0~999 로 넓어졌으므로 나눗수로 눌러야 하는데, 너무 세게 누르면
기체 기동(MOB)이 파일럿 능력을 압도해 명중률이 16% 까지 떨어진다
([함정 §11.12](#1112-능력치-스케일을-바꾸면-명중식-비율이-무너진다)).

`clamp(±6)` 은 그대로 유지한다. ±6은 명중률 **11%~89%** 구간이다.

### 8.6 크리티컬

```
필요마진 = clamp(9 − floor(사격|격투/60) − floor(정신/150)
                 + floor(상대반응/90) − round(무장크리/5), 3, 12)
마진 ≥ 필요마진 → 크리티컬, 피해 ×1.45
사신 발동 시에는 마진과 무관하게 크리티컬 확정
```

구 '기량' 이 없어졌으므로 그 자리를 실제로 쏘는 능력치와 **정신**이 대신한다.

### 8.7 피해

```
공격력 = ATK × (1 + 사격|격투 × ATK_STAT_K) × 기력배율 × 전법
방어력 = DEF × (1 + 수비 × DEF_STAT_K) × (1 + (기력 − 100) × 0.002)
         × (저력 중이면 1.30)

피해 = 무장위력 × (공격력 / 방어력) × DMG_K × 피격측전법
       × (피격측이 desperate 면 1 + 0.12 × penMul)
       × (공격자가 적이면 FOE_DMG)
       × (방어측이 대형기면 1.06)

최소 200, 10단위 반올림
```

**`DEF_STAT_K` 를 `ATK_STAT_K` 보다 낮게 둔 것은 의도적이다.**
같게 두면 양쪽이 스탯을 올릴수록 서로 안 죽는 교착이 생긴다.

> 능력치 상한이 20 에서 999 로 바뀌면서 계수도 같이 내려갔다.
> `0.02 → 0.00167`, `0.018 → 0.0015`. 999 에서 각각 ×2.67 / ×2.50 이 되어
> 구 체계(20에서 ×1.40 / ×1.36)보다 성장 체감은 커지되 비율은 유지한다.

### 8.8 기력 (모랄)

```
morB(u) = clamp(floor((기력 − 100) / 10), −4, +5)   // 명중/회피 보정
morP(u) = 1 + (기력 − 100) × 0.005                   // 화력 배율 (150 → ×1.25)
```

| 사건 | 변화 |
|---|---|
| 공격 명중 | 공격측 **+1 + floor(정신/100)** |
| 피격 | 피격측 +2 |
| 회피 성공 | 회피측 +1 |
| 격추 달성 | 공격측 +3 |
| 아군 격추당함 | 같은 편 생존자 전원 +5 |

범위 50~150. 전투 개시 기력은 `100 + floor(정신/25)` 로 시작한다.

### 8.9 적 유닛 생성

적 파일럿 능력치는 **플레이어 레벨이 아니라 실제 능력 총합**을 따라간다.
포인트 자율 분배 때문에 같은 레벨이라도 실력 차가 크기 때문이다.

```js
base = max(4, round(statTotal() / 6 × 0.92))
off  = round(base × (보스 1.45 / 에이스 1.10 / 잡졸 1.00))   // 사격·격투
eva  = round(base × (보스 1.05 / 에이스 1.00 / 잡졸 0.86))   // 반응
awk  = round(base × (보스 1.10 / 에이스 0.80 / 잡졸 0.40))
rea  = round(eva × (1 − 카리스마 감소율))          // 플레이어 스킬이 여기서 먹는다
spi  = round(base × 0.5)                          // 적기도 저력을 쓴다
무장레벨 = clamp(1 + floor(레벨/5), 1, 5)

HP  = 기본HP  × FOE_HP[등급]                  // 잡졸 0.38 / 에이스 0.52 / 보스 0.88
ATK = 기본ATK × 임무난이도 × 지형계수 × (보스 1.45)
DEF = 기본DEF × (보스 1.15)
MOB = 기본MOB × 지형계수
EN  = 기본EN                                   // 매 라운드 10% 회복하므로 배수 불필요
기력 = 에이스·보스 110, 잡졸 100
```

**`off` 와 `eva` 를 분리한 것이 이 설계의 핵심 교훈이다.** 하나의 값으로 둘 다
굴리면 강한 적일수록 "절대 안 맞고 절대 안 빗나가는" 무적이 된다
([§11.2](#112-정예기가-무적이-되는-이유)).

**지휘기는 HP 가 아니라 화력으로 세운다.** HP 를 1.15배까지 올려 봤더니
24라운드 안에 결판이 안 나 무승부가 30% 까지 치솟았다. HP 를 0.88 로 되돌리고
ATK 를 1.45배로 올리자 무승부가 2~14% 로 떨어지고 패배가 실제로 나오기 시작했다.

보상:

```
자금 = 시세 × 0.05 × (보스 4 / 에이스 2 / 잡졸 1)
EXP  = (10 + 등급인덱스 × 26) × (보스 5 / 에이스 2.5 / 잡졸 1)
```

### 8.10 임무

| ID | 이름 | AP | Lv | 적 편성 | 난이도 | 기본 보수 |
|---|---|---|---|---|---|---|
| `ptrl` | 초계 임무 | 1 | 1 | N·N·R 중 2기 | 0.80 | 9,000C |
| `swp` | 소탕전 | 2 | 3 | R·R·SR 중 3기 | 0.85 | 27,000C |
| `base` | 거점 공략 | 2 | 7 | SR·SR·SSR 중 4기 | 0.95 | 64,000C |
| `itcp` | 에이스 요격 | 3 | 12 | SSR 2기 + UR 에이스 | 1.00 | 155,000C |
| `final` | 결전 · 기함 요격 | 4 | 18 | SSR/UR 3기 + UR 지휘기 | 1.20 | 430,000C |

**적 편성은 매 출격마다, 그 전장에 적성이 있는 기체 중에서 새로 뽑는다.**
해당 등급 풀이 비면(예: 수상의 UR) 전장 전체 풀로 폴백한다.

보스는 UR 풀에서 `HP + ATK×40` 상위 5기 중 하나를 무작위로 고른다.
(ATK 가 10% 로 줄었으므로 가중치도 `×4 → ×40` 으로 같이 옮겼다.)

### 8.11 승패와 정산

| 결과 | 조건 | 정산 |
|---|---|---|
| 완수 | 적 전멸 | 격추 보상 + 임무 보수, 명성 +4~10 (기함 첫 격파 시 추가 +30) |
| 대파 | 플레이어 HP 0 | 격추 보상 ×0.4, **보유 자금 −10%**, HP는 1로 생존 |
| 철수 | 24라운드 초과 | 격추 보상 ×0.7 |

남은 HP는 다음 출격까지 유지되므로 **정비 타이밍이 자원 관리의 축**이 된다.

---

## 9. 밸런스 — 상수와 근거

### 9.1 상수표 (`src/rules.mjs` 상단)

| 상수 | 값 | 역할 |
|---|---|---|
| `ROUND_CAP` | 24 | 라운드 상한. 넘으면 철수 |
| `DMG_K` | 3.0 | 전역 피해 계수 |
| `FOE_HP` | 잡졸 0.38 / 에이스 0.52 / 보스 0.88 | 적 HP 배수 |
| `FOE_DMG` | 0.55 | 적→아군 피해 계수 |
| `ATK_STAT_K` | 0.00167 | 사격·격투 1점당 화력 상승률 (999 → ×2.67) |
| `DEF_STAT_K` | 0.0015 | 수비 1점당 장갑 상승률 (999 → ×2.50) |
| `STAT_HIT_K` | 6 | 명중식에서 캐릭터 능력치 나눗수 |
| `MOB_EVA_K` | 350 | 명중식에서 기체 기동 나눗수 |
| `HIT_CLAMP` | 6 | 명중 우열 상한 (명중률 11~89%) |
| `EN_REGEN` | 0.10 | 매 라운드 EN 회복률 |
| `DIST_MIN`/`DIST_MAX` | 1 / 20 | 교전 거리 |
| `RANGE_MUL` | 2 | 원 사거리 → 거리축 환산 배수 |
| `AWK_FORESEE` | 800 | 미래 예측 발동 하한 |
| `AWK_FORESEE_P` | 0.05 | 미래 예측 확률 |
| `AWK_RANGE_CAP` | 13 | 각성 무장 사거리 상한 |
| `START_PT` / `LVUP_PT` | 120 / 5 | 파일럿 능력 포인트 |
| `ULV_MAX` / `ULVUP_PT` | 9999 / 4 | 기체 레벨 · 레벨당 강화 포인트 |
| `PSK_MAXLV` / `PSK_SLOT` | 10 / 3 | 스킬 레벨 상한 · 장착 슬롯 |
| `PART_SLOT` / `PART_PRICE` | 3 / 220,000 | 파츠 슬롯 · 상점가 |
| `SPI_PEN_MAX` | 0.30 | 정신 999 에서의 패널티 경감률 |
| `SPI_CRIT_K` | 150 | 정신 몇 점당 크리티컬 필요 마진 −1 |
| `GUTS_HP` / `GUTS_TURN` / `GUTS_MUL` | 0.30 / 10 / 1.30 | 저력 발동선 · 지속 · 배수 |
| `SEED_BONUS` | 0.20 | SEED 발동 시 각성·반응 상승률 |
| `ORDER_MARKUP` | 1.15 | 상점 발주 웃돈 |

### 9.2 기체 제원 규격 (#19~#22)

원 데이터의 공격·방어·기동은 **10%(소수점 버림)** 로 줄여서 담는다.
수집 단계에서 한 번만 하고, 이후로는 이 값이 원본처럼 취급된다.

```
공격력 3,780 → 378        상한 9,999
HP    그대로              상한 999,999
EN    그대로              상한 999
색적  파생값              상한 9,999
```

**피해식은 `ATK / DEF` 비율이라 균일한 10% 축소로는 아무것도 변하지 않는다.**
바뀌는 것은 명중식의 `floor(MOB / MOB_EVA_K)` 뿐이라 나눗수만 같이 옮기면 된다.

등급별 중앙값 (`fetch-roster.mjs` 가 마지막에 출력):

| 등급 | HP | ATK | DEF | MOB | EN | 색적 | 최고위력 | 무장수 |
|---|---|---|---|---|---|---|---|---|
| N | 39,568 | 415 | 376 | 396 | 184 | 503 | 3,960 | 2 |
| R | 48,700 | 511 | 465 | 485 | 219 | 584 | 4,080 | 2 |
| SR | 58,571 | 615 | 558 | 583 | 254 | 691 | 4,440 | 3 |
| SSR | 74,285 | 816 | 731 | 738 | 315 | 839 | 5,040 | 3 |
| UR | 100,155 | 1,062 | 953 | 1,002 | 417 | 1,049 | 6,480 | 4 |

**HP : ATK 비가 등급 전체에서 거의 일정하다.** 그래서
`무장위력 × (ATK/DEF) × K` 형태의 식 하나로 N부터 UR까지 비슷한 히트 수가 나온다.
**계수를 다시 잡아야 할 때는 이 표를 먼저 뽑아라.**

### 9.3 목표 승률

전장 × 임무별 40회 시뮬레이션. 각 임무를 **그 임무에 맞는 장비**
(해금 레벨, 해당 전장 ○ 적성기 중 등급 중앙값, 무장 Lv, 개조 단수)로,
**스킬 없이** 돌렸다. 전법은 통상.

| 임무 | 우주 | 지상 | 공중 | 수중 | 수상 |
|---|---|---|---|---|---|
| 초계 임무 | 85% | 93% | 90% | 100% | 85% |
| 소탕전 | 70% | 80% | 55% | 85% | 70% |
| 거점 공략 | 53% | 53% | 40% | 75% | 53% |
| 에이스 요격 | 50% | 60% | 68% | 75% | 38% |
| 결전 · 기함 요격 | 68% | 63% | 88% | 63% | 48% |

무승부는 전 구간 10% 이하다. 장비가 한 단계 뒤처지면 승률이 급락한다 —
결전을 SSR 로 들어가면 **0%**, UR 로 들어가면 56~70%. 이게 암시장에서
기체를 갈아타야 할 이유다.

**수상·수중이 튀는 것은 원 데이터의 적성 분포 때문이다**([7.4](#74-전장과-지형-적성-1316)).
수상은 ○ 기체가 44기뿐이라 플레이어 기체가 약하고, 수중은 양쪽 다 △ 라
공격·기동이 함께 깎여 초·중반이 느슨해진다. 밸런스 문제가 아니라
**전장 선택이 실제 전략이 된다는 뜻**이다.

### 9.3-b 파일럿 스킬의 무게

같은 장비·같은 레벨에서 스킬만 얹어 측정한 우주 전장 승률
(스킬 없음 50회 · 나머지 40회):

| 임무 | 스킬 없음 | 뉴타입·카리스마·포커 Lv3 | 뉴타입·카리스마·포커 Lv10 |
|---|---|---|---|
| 초계 임무 | 86% | 83% | 93% |
| 소탕전 | 62% | 65% | 95% |
| 거점 공략 | 60% | 73% | 90% |
| 에이스 요격 | 58% | 50% | 90% |
| 결전 · 기함 요격 | 56% | 68% | 95% |

같은 조건에서 **광전사·사신·메테오 Lv10** 은 98/95/88/83/100%,
**SEED·사격전문가·철벽 Lv10** 은 90/85/90/60/78% 로, 조합에 따라 성격이 갈리되
어느 하나가 지배하지는 않는다.

Lv3 은 오차 범위를 조금 웃도는 수준이고, **Lv10 이 되어야 판을 뒤집는다.**
세 스킬을 Lv10 까지 올리는 데 **41,652,000C** 가 든다 — 결전 임무 보수
430,000C 기준 약 97회분. 스킬은 최종 목표지 초반 해법이 아니다.

### 9.4 전법 균형

거점~요격 구간 기준. 지배 전략이 없도록 맞췄다.

| 전법 | 보정 | 성격 |
|---|---|---|
| 신중 | 명중 +2 / 화력 −10% / 피해 −18% | 안정적, 장기전 |
| 통상 | 없음 | 기준선 |
| 회피 | 회피 +4 / 화력 −18% / 피해 −12% | 패배가 적은 대신 무승부가 는다 |
| 맹공 | 명중 −2 / 화력 +22% / 피해 +28% | 빠르지만 대파 위험 |

> 초안에서는 맹공이 압도적이었다. 화력을 1.28→1.22로 낮추고
> 피격 배수를 1.10→1.28로 올려 위험을 실제 비용으로 만들었다.

---

## 10. 검증 도구

### 10.1 밸런스 시뮬레이터

```bash
node test/sim.mjs
TERS=sp,gr,ai,uw,se N=50 node test/sim.mjs
```

**서버를 띄우지 않는다.** 규칙 모듈을 그대로 `import` 해서 전투만 대량으로 돌린다.

```js
import * as R from '../src/rules.mjs';
R.initRules(JSON.parse(fs.readFileSync('../src/roster.json', 'utf8')));
...
const out = R.resolveBattle(R.MISSION.find(m => m.id === mid), 'norm', ter);
if (out.res.r === 'win') w++;
```

> v4 까지는 `engine.js` 가 DOM 과 `setTimeout` 을 붙들고 있어서 `vm` 컨텍스트에
> 가짜 `document` 와 즉시 실행 `setTimeout` 을 심어야 했다. 규칙을 분리한 뒤로는
> 그냥 `import` 하면 된다. **서버로 옮기면서 딸려 온 실질적인 이득이다.**

### 10.2 API 종단 점검

```bash
node server.mjs        # 다른 터미널
node test/e2e.mjs
BASE=https://내주소 node test/e2e.mjs
```

브라우저 없이 HTTP 만으로 한 판을 돌린다. 정상 동작뿐 아니라
**클라이언트가 거짓말을 해도 서버가 막는지**를 함께 본다.

```
OK  합계 틀린 new 거절        초기 능력치 합계는 120 이어야 합니다.
OK  계급/자금 부족 발주 거절    계급이 부족합니다.
OK  오늘 매물 아닌 것 거절      오늘 매물이 아닙니다.
OK  보유 초과 배분 거절        보유 포인트를 넘었습니다.
```

이 네 줄이 v5 의 존재 이유다. v4 에서는 콘솔에서 `g.cash = 1e9` 한 줄이면
끝났지만, 지금은 서버가 자기 세이브를 보고 판단한다.

### 10.3 이관 회귀 시험 (일회성)

v4 → v5 로 규칙을 옮길 때 쓴 방법이다. 승률 비교로는 표본 오차에 묻히므로
**결정론적 함수를 1:1 로 대조**했다 ([§5.4](#54-이관이-정확한지-어떻게-확인했나)).

```js
/* 구 engine.js 를 vm 에 올리고, 같은 입력·같은 난수열을 먹인다 */
const oldRnd = mk(seed), newRnd = mk(seed);
ctx.Math.random = oldRnd; Math.random = newRnd;
eq('mkPlayer', V('...mkPlayer(tac, ter)...'), JSON.stringify(R.mkPlayer(tac, ter)));
```

`Math` 를 `Object.create(Math)` 로 감싸 `vm` 컨텍스트에 넣으면 `random` 만
바꿔치기할 수 있다. 규칙을 크게 손볼 때 같은 방법을 다시 쓸 수 있다.

---

## 11. 함정 모음

실제로 밟은 것들. 다시 만들 때 여기서 시간을 아낄 수 있다.

### 11.1 `per_page` 가 100에서 잘린다

`per_page=5000` 을 넣어도 100건만 온다. `total_pages` 를 보고 순회하지 않으면
1244기 중 100기만 받고 "전체 UR 등급"이라는 엉뚱한 통계를 얻게 된다.

### 11.2 정예기가 무적이 되는 이유

가장 오래 잡아먹은 버그. 적 파일럿 능력치를 **하나의 값 `ps` 로 만들어
사격·격투·반응에 전부 넣었더니**, 레벨이 오를수록 보스가

- 플레이어를 향한 명중률 **100%**
- 플레이어에게 맞을 확률 **5%**

인 상태가 됐다. 강한 적일수록 공격과 회피가 **동시에** 좋아지니 당연한 결과다.
`off`(공격)와 `eva`(회피)를 분리하고, 정예 보너스를 공격 쪽에 몰아주고
회피 쪽은 +1만 주는 것으로 해결했다.

### 11.3 명중 우열에 상한이 없으면 교착한다

`hitAdv` 를 그대로 두면 고레벨에서 우열이 −13까지 벌어져 명중률이 0.8%가 된다.
양쪽 다 안 맞아서 20라운드 내내 아무 일도 안 일어나고 전부 무승부가 된다.
`clamp(±6)` 으로 11~89% 구간에 가둬야 전투가 성립한다.

### 11.4 무장 레벨 1 기준으로 계산할 것

밸런스를 잡을 때 `pw[len-1]`(최대 레벨) 기준으로 계수를 뽑았는데, 플레이어는
**Lv1으로 시작한다.** 실제 초반 화력이 예상보다 20% 낮아 첫 임무가 무승부로
끝났다. 시뮬레이터를 `wlv = 1`과 `5` 양쪽으로 돌리는 이유다.

### 11.5 전투 종료 시 로그가 사라진다

전투가 끝나면 결과 상자를 그리려고 `renderAll()` 을 부르는데, 이때
`viewBattle()` 이 빈 `#blog` 를 새로 만들어 **방금 본 42줄짜리 전투 기록이
통째로 날아간다.** 로그와 상황판을 `S.blog[]` / `S.board` 에 보관해 두고
`viewBattle()` 이 그걸 다시 뿌리도록 고쳤다.

### 11.6 배경 탭에서 `setTimeout` 이 throttle된다

전투 연출은 `sleep()` = `setTimeout` 기반이다. 브라우저가 **비활성 탭의
타이머를 1초 간격으로 늦추므로**, 탭을 뒤로 둔 채 자동 시뮬레이션을 돌리면
수십 배 느려져 타임아웃 난다. §10 시뮬레이터가 DOM·타이머를 전혀 안 쓰는
동기 루프인 이유가 이것이다.

### 11.7 이미지를 HTML 에 내장하면 규모가 막힌다

base64 내장은 단일 파일이라는 장점 대신 **×1.34 팽창 + 지연 로딩 불가 + 용량 상한**을
같이 가져온다. 110기까지는 괜찮았지만 1244기에서는 성립하지 않는다.
파일로 분리하면 세 문제가 한꺼번에 사라진다([4.4절](#44-이미지는-왜-분리하는가)).

거꾸로, **데이터를 별도 JSON 으로 빼서 `fetch` 하는 것은 하지 말 것.**
`file://` 에서 CORS 에 막혀 로컬 실행이 깨진다. 이미지는 `<img src>` 라 상대경로가
그대로 먹지만 `fetch` 는 다르다. 그래서 데이터는 HTML 에 인라인으로 둔다.

### 11.8 등급을 지울 때 남는 것

화면에서 등급(N·SR·UR)을 빼도 **가격·해금 레벨·적 편성 풀**이 등급을 키로 쓰고 있다.
표기만 지우고 `rar` 는 내부 키로 남겨 두는 편이 안전하다. 대신 등급 탭으로
쓰던 개발·도감 화면의 분류 수단이 사라지므로 **작품(시리즈) 필터로 갈아끼워야** 한다.

### 11.9 Artifact CSP 는 외부 리소스를 전부 막는다

아티팩트로 발행하면 **외부 이미지가 호스트 불문하고 차단**되고 `fetch`/XHR 도 막힌다.
조용히 실패해서 빈 칸만 보인다. 즉 "아티팩트는 두고 이미지만 깃허브에서 불러오기"는
불가능하다. 아티팩트를 쓰려면 base64 내장 + 16MB 상한(약 500기)을 받아들여야 하고,
그 이상은 GitHub Pages 같은 일반 정적 호스팅으로 가야 한다.

### 11.10 사전을 고쳤다고 이미지까지 다시 받지 말 것

한글화는 수집 시점에 적용되므로 사전을 고치면 재수집이 필요하다. 하지만 이미지는
바뀌지 않았다. `--skip-images` 를 주면 상세 API 만 다시 돌아 **1분**이면 끝난다.
이걸 모르면 매번 133MB 를 다시 받게 된다.

### 11.11 로스터를 늘리면 사전 커버리지가 무너진다

110기 → 1244기로 늘렸을 때 고유 무장이 223종 → 1226종이 되면서 번역률이
**48%로 떨어졌다.** 시리즈도 43종 → 103종이 됐다. 규모를 바꾼 뒤에는 반드시
커버리지를 다시 재고([4.5절](#45-한글화)) 사전을 보강할 것.

---

### 11.12 능력치 스케일을 바꾸면 명중식 비율이 무너진다

능력치 상한을 20에서 999로 올릴 때 명중식의 캐릭터 항만 `floor(stat/12)` 로
누르고 기체 기동 항은 비례해서 `floor(MOB/220)` 으로 옮겼다. 결과:

```
레벨 20 · 사격 40   → floor(40/12)  = 3
UR 기동 1,000       → floor(1000/220) = 4
```

**파일럿 능력치보다 기체 기동이 커져 버렸다.** 실측 명중률 16%, 무승부 70%.
`floor(stat/6)` + `floor(MOB/350)` 으로 비율을 되돌리자 무승부가 0~2% 로 떨어졌다.

교훈 — **스케일을 바꿀 때는 개별 항이 아니라 항끼리의 비를 먼저 보라.**
`STAT_HIT_K` 와 `MOB_EVA_K` 를 상수로 뽑아 둔 이유다.

### 11.13 거리 개념을 넣으면 무승부가 폭증한다

교전 거리를 1~20 무작위로 잡았더니 절반 이상의 전투가 "아무도 사거리에
닿지 않는" 상태로 시작했다. 양쪽이 각자 대항판정으로 1~3칸씩 좁히다 보니
20라운드 중 5~7라운드가 이동에만 쓰였다.

**양쪽이 같은 방향을 원할 때는 대항판정을 하지 않는다.** 서로 달려드는
상황이므로 속도를 합산해 한 번에 좁힌다.

```js
if (pDir === eDir) step = pDir * (1 + Math.floor((mvSpd(P) + mvSpd(fLead)) / 4));
```

이것만으로 평균 라운드가 17.4 → 10.3 으로 줄었다.

### 11.14 보스 HP 를 올리면 강해지는 게 아니라 안 끝난다

지휘기가 너무 쉬워서 HP 배수를 0.72 → 1.15 로 올렸다. 승률은 82%→56% 로
내려갔지만 **패배가 아니라 무승부가 늘었다**(0% → 30%). 라운드 상한 안에
깎아낼 수 없는 HP 덩어리가 됐을 뿐이다.

HP 를 0.88 로 되돌리고 대신 **ATK 를 1.45배**로 올리자 같은 승률에서
무승부가 2~14%, 패배가 22% 로 정상화됐다. **라운드 상한이 있는 전투에서
난이도는 HP 가 아니라 화력으로 올려야 한다.**

### 11.15 숨은 유닛이 영구히 숨는다

색적(시야 이탈)을 넣자 기동이 높은 기체가 매 라운드 은신 판정을 통과해
아무도 때릴 수 없는 상태가 이어졌다. **공격하면 위치가 드러나도록**
(`A.hidden = false; A.exposed = true`) 하고, `exposed` 인 라운드에는 다시
숨지 못하게 막아야 성립한다.

### 11.16 등급을 지웠으면 가격도 등급에서 뽑으면 안 된다

`PRICE = {N:38000, ... UR:990000}` 처럼 등급별 정가표를 쓰면 화면에서 등급을
지워도 **가격이 등급을 그대로 노출한다.** 제원 총합에서 뽑도록 바꿨다.

```js
uPow(B) = B.hp*0.55 + (B.atk*3 + B.def*2.2 + B.mob*2.2 + B.sct*1.6)*22 + B.en*140
시세    = round(pow(uPow/1000, 1.6) * 90 / 100) * 100
```

지수 1.6 은 가격 폭을 벌리기 위한 것이다. 선형으로 두면 N 228k ~ UR 548k 로
2.4배밖에 안 벌어져서 "비싼 기체를 사는 재미"가 나오지 않는다. 현재 4.1배.

### 11.17 `display:flex` 는 `[hidden]` 을 이긴다

이미지 확대 오버레이를 이렇게 썼다가 **페이지 전체가 계속 어둡게 덮였다.**

```css
#zoom{position:fixed;inset:0;background:rgba(0,0,0,.88);display:flex}
```

브라우저 기본 스타일시트의 `[hidden]{display:none}` 은 그냥 작성자 스타일이라,
같은 명시도의 `#zoom{display:flex}` 가 **id 선택자 우위로 이겨 버린다.**
`el.hidden = true` 를 해도 `display:flex` 가 남는다. 명시적으로 눌러야 한다.

```css
#zoom[hidden]{display:none}
```

`hidden` 속성으로 껐다 켜는 요소에 `display` 를 지정할 때는 **항상** 이 한 줄을
같이 쓸 것. 증상이 "안 보여야 할 게 보인다"가 아니라 "화면이 어둡다"로 나와서
원인을 찾기 어렵다.

### 11.18 명중률을 '30% 상승' 시키라는 요구

2d6 대항판정에는 곱할 확률이 없다. 우열값에 `+2` 를 주는 식으로 뭉개면
구간에 따라 실제 상승률이 25%~40% 로 흔들린다.

**확률에 배수를 걸고, 그 확률에 가장 가까운 우열값으로 되돌린다.**

```js
const advScale = (adv, mul) => advForP(clamp(oppP(adv) * mul, 0.02, 0.98));
```

`OPP` 표가 이미 우열 → 확률 매핑이므로 역방향 탐색은 29칸 선형 스캔이면 된다.
주사위 굴림과 로그 표기를 그대로 두면서 요구 수치를 지킬 수 있다.

### 11.19 스킬 상승분과 하락분을 한 값으로 합치지 말 것

광전사는 "사격·격투·반응 +30% / 수비 −20%" 다. 이걸 `st.def += -0.20` 으로
합쳐 두면, **정신·포커페이스가 하락분을 깎아 줘야 할 때 손댈 수가 없다.**

```js
x.st[k]   // 순수 상승률
x.pst[k]  // 하락률 — penMul 이 곱해진다
최종 = g.st[k] * (1 + x.st[k] − x.pst[k] * penMul)
```

같은 이유로 전법도 양(+) 보정과 음(−) 보정을 나눠 처리한다. 신중의 명중 +2 는
그대로 두고 화력 −10% 만 완화한다. 안 그러면 정신이 전법 선택을 무의미하게 만든다.

### 11.20 사전을 고쳐도 로스터는 안 따라온다

`'무장'` 으로 표기를 바꾸랬는데 화면 어딘가에 계속 '병장'이 남아 있었다.
원인은 `i18n.mjs` 의 `Weapon: '병장'` 이 **빌드 시점에 이미 roster.json 에
박혀 있었기** 때문이다. 사전만 고치면 다음 재수집 때까지 반영되지 않는다.

재수집(3~5분)이 아까우면 순수 문자열 치환이므로 직접 바꿔도 된다.

```bash
node -e "const fs=require('fs');fs.writeFileSync('src/roster.json',
  fs.readFileSync('src/roster.json','utf8').split('병장').join('무장'))"
```

**단, `i18n.mjs` 도 같이 고쳐야 한다.** 안 그러면 다음 재수집에서 되돌아온다.

### 11.21 여러 줄 주석 한가운데를 자르면 뒤가 통째로 먹힌다

`rules.mjs` 를 브라우저 번들에 넣을 때 맨 끝 `export` 블록을 잘라내야 한다.
처음에는 `lastIndexOf('export {')` 로 잘랐는데, 그 앞 주석이 이랬다.

```js
/* =========================================
   내보내기 — build.mjs 는 이 블록을 떼고 …
   /* @@EXPORTS@@ */
export { ... };
```

중첩 `/*` 는 **첫 `*/` 에서 주석이 끝난다.** 그 지점을 자르면 열린 주석이 남아
바로 뒤에 붙인 `initRules(ROSTER);` 를 통째로 삼켰다. 증상은
`UNITS.length === 0` — 로스터를 1244개 실어 놓고 목록이 텅 비었다.

절단 표시는 **반드시 한 줄짜리 주석**으로 두고, 그 줄을 통째로 찾는다.

```js
const MARK = '/* @@BROWSER_CUT@@';
const rulesBrowser = rules.slice(0, rules.indexOf(MARK));
```

### 11.22 모듈 전역 상태를 서버에서 쓰려면 '동기'가 조건이다

`rules.mjs` 는 세이브를 모듈 지역 변수 `g` 하나로 들고 `setState()` 로 갈아 끼운다.
40개 함수에 `g` 를 인자로 흘리지 않아도 되니 이관이 훨씬 안전했지만,
여러 플레이어가 붙는 서버에서는 위험해 보인다.

**안전한 이유는 모든 진입점이 완전히 동기이기 때문이다.** Node 는 동기 실행
중간에 다른 요청으로 넘어가지 않는다. 그래서 `applyAction()` 안에서 `await` 가
한 번도 나오지 않는 한 상태가 섞일 수 없다.

```js
R.setState(g);
try { return { out: fn(g, a), g: R.getState() }; }
finally { R.setState(null); }        // 다음 요청에 남기지 않는다
```

이 불변식이 깨지는 순간(예: 전투 중에 DB 를 읽으려고 `await` 를 넣는 순간)
플레이어끼리 세이브가 섞인다. `rules.mjs` 와 `actions.mjs` 상단에
**"이 파일에서는 await 를 쓰지 말 것"** 을 못 박아 둔 이유다.

### 11.23 서버로 옮기면 사라지는 것 — 마이그레이션

v4 의 `load()` 는 세이브를 꺼내면서 누락 필드를 채우는 일까지 했다.
그 함수를 클라이언트에서 걷어내자 **마이그레이션도 같이 사라졌다.**
새 세이브는 `newGame()` 이 모든 필드를 만들어 주므로 한동안 티가 안 난다.

세이브를 만드는 주체가 바뀌면 **마이그레이션이 어디로 가야 하는지 먼저 정할 것.**
지금은 `rules.mjs` 의 `migrate()` 가 맡고, 저장소에서 꺼낼 때마다 통과시킨다.

```js
const load = async (store, pid) => R.migrate(await store.get(pid));
```

### 11.24 서버리스에서 DB 클라이언트는 모듈 전역에 캐시할 것

Vercel 함수는 인스턴스를 재사용한다. 요청마다 `new MongoClient(...).connect()`
를 하면 커넥션이 폭발한다. 모듈 전역에 두고 재사용하되,
**연결에 실패한 클라이언트를 전역에 남기면 안 된다** — 다음 요청이 죽은 연결을
그대로 물게 된다.

```js
const c = new mod.MongoClient(uri, { maxPoolSize: 10, serverSelectionTimeoutMS: 8000 });
try { await c.connect(); }
catch (e) { await c.close().catch(() => {}); throw new Error('MongoDB 에 연결하지 못했습니다 — ' + e.message); }
mongoClient = c;      // 성공한 뒤에야 전역에 올린다
```

### 11.25 액션 이름과 액션 인자가 같은 키를 쓰면 안 된다

액션을 `{ k: 'skbuy', k: 'nt' }` 처럼 보내려다 막혔다. 라우팅 키가 `a.k` 인데
스킬 키도 `a.k` 였기 때문이다. 개조(`a.k`)·파츠(`a.k`)도 같이 겹쳤다.

인자 이름을 나눴다 — 스킬 `a.sk`, 개조 `a.mk`, 지형 `a.ter`, 능력치 `a.stat`.
라우팅에 쓰는 이름은 예약어처럼 다루는 편이 낫다.

---

## 12. 스타일 가이드

목표는 **2000년대 초 한국 CGI 게시판 게임**의 질감이다. 세련되게 만들면 안 된다.

### 12.1 색 토큰 (`src/head.html` 의 `:root`)

```css
--bg:#000            /* 순검정 배경 */
--panel:#07070b  --panel2:#0d0d14
--row:#101018    --row2:#0a0a10      /* 표 줄무늬 */
--bd:#3a3a4c     --bd2:#22222e       /* 표 테두리 2단계 */
--tx:#c8c8d0     --dim:#7a7a8a       /* 본문 / 흐린 글씨 */
--cy:#3fe0ff     --lime:#5cff5c      /* 형광 시안 / 라임 */
--yel:#ffe400    --org:#ff9d2e
--red:#ff4444    --mag:#ff6cc4
--lnk:#7fc9ff                        /* 링크 */
/* 등급색 */
--n:#9aa0ac  --r:#5aa0ff  --sr:#b07cff  --ssr:#ffc83a  --ur:#ff6a3d
```

### 12.2 서체

```css
--f-ui: "Dotum","돋움","Gulim","굴림","Malgun Gothic",sans-serif;  /* 시스템 우선 */
--f-mn: "Nanum Gothic Coding","D2Coding","Consolas",monospace;      /* 수치·로그 */
--f-dp: "Black Han Sans","Dotum",sans-serif;                        /* 로고 */
```

**본문에 시스템 폰트를 먼저 두는 게 핵심이다.** Windows에서 실제 돋움/굴림이
잡혀야 그 시절 질감이 난다. 웹폰트는 로고와 등폭 두 곳에만 쓴다.

### 12.3 구성 규칙

- 프레임셋 흉내: 좌측 300px 스테이터스 + 우측 메인 + 하단 커맨드 바
- 섹션 제목은 전부 `【 】` 로 감싼다. 공지는 `※` 로 시작한다
- 모든 정보는 `<table>`. 카드·둥근 모서리·그림자 금지 (`border-radius` 0)
- 게이지는 `repeating-linear-gradient(90deg, ... 0 3px, ... 3px 4px)` 로
  도트 찍힌 막대를 만든다. 매끈한 그라디언트를 쓰면 시대감이 깨진다
- 배경에 4px 간격 스캔라인 두 겹 + 상단 라디얼 그라디언트
- 링크 hover는 `color:#ff4444; background:#2a0000` — 당시 흔하던 반전 효과

---

## 13. 커스터마이즈 레시피

### 로스터 규모

기본이 전 기체(1244기)다. 줄이려면 `--limit=<n>` (등급별 n기).

```bash
node src/fetch-roster.mjs --limit=40   # 약 200기
```

**1기당 데이터 약 760B + 이미지 118KB.** 데이터는 전부 담아도 1MB 미만이라
규모를 좌우하는 건 사실상 이미지뿐이다.

### 데이터만 다시 받기

제원 스키마(지형 적성·색적·능력치 배율)를 바꿨는데 이미지는 그대로일 때:

```bash
node src/fetch-roster.mjs --skip-images   # 이미 있는 이미지는 건너뛴다
node build.mjs
```

1244기 상세 조회에 3~5분 걸린다. 끝나고 **반드시 기수를 확인할 것** —
API 가 몇 건 실패하면 로스터가 조용히 줄어든다.

```bash
node -e "console.log(require('./src/roster.json').length)"   # 1244 여야 한다
```

### 특정 기체 찾기

전 기체를 담으므로 강제 포함 목록은 필요 없다. API 의 정확한 이름을 확인하려면:

```bash
node --eval "fetch('https://ggendb.up.railway.app/api/units?lang=EN&page=1&per_page=100&q=Sazabi').then(r=>r.json()).then(j=>console.log(j.rows.map(r=>r.name+' / '+r.rarity)))"
```

### 임무 추가

`src/rules.mjs` 의 `MISSION` 배열에 항목을 넣는다. 상황실·출격 화면이 자동으로
순회하므로 UI 수정은 필요 없다.

```js
{ id:'raid', n:'거점 습격', ap:3, lv:16, pool:['SSR','UR'], cnt:5,
  diff:1.08, pay:280000, exp:900, d:'적 보급 거점을 급습한다.' }
```

`ace:'UR'` 을 넣으면 마지막 1기가 에이스로, `boss:true` 를 함께 넣으면 지휘기로 바뀐다.
편성은 **선택한 전장에 적성이 있는 기체 중에서만** 뽑히므로 별도 설정은 없다.

### 전장 추가

`TERRAIN` 에 키를 넣고 `TER_ORDER` 에 순서를 더한 뒤, 그 키를 `roster.json` 의
`tr` 에 채워야 한다. **API 가 주는 지형은 5종뿐**이므로 새 전장을 만들려면
`fetch-roster.mjs` 의 `TERKEY` 에서 기존 지형을 조합해 파생시켜야 한다.

파츠는 `TER_ORDER` 를 순회해 자동 생성되므로 따로 추가할 것이 없다.

### 파일럿 스킬 추가

`src/rules.mjs` 의 `PSKILL` 에 항목 하나를 더하면 상점·장착 UI·효과 집계가
모두 이 배열을 순회하므로 다른 곳은 손댈 필요가 없다.

```js
{ k: 'ace', n: '에이스의 감', d: '명중 +1/Lv',
  e: (L, x, c) => { x.hitFlat = (x.hitFlat || 0) + L; } }
```

`e(L, x, ctx)` 의 인자 —
`L` 은 스킬 레벨(`nolv:true` 면 항상 1), `x` 는 효과 객체, `ctx` 는
`{ eq, sk, plv, isGundam }`. `x` 에 새 필드를 만들었다면 `mkPlayer()` 에서
읽어 태워야 한다.

```js
hitMod: hitMod + (e.hitFlat || 0),
```

출신 계통으로 묶어 배타 처리하려면 `g:'origin'` 을 붙인다.
전투 중 일시 보정은 `awkTmp` 에 넣으면 **미래 예측 판정에도 그대로 반영된다.**

### 기체 레벨 곡선

```js
const uExpNeed = lv => Math.round(80 * Math.pow(lv, 1.32));
const ULVUP_PT = 4;
```

`ULVUP_PT` 를 바꿀 때는 반드시 아래를 확인할 것 —
**레벨업만으로 전 항목 최대에 도달할 수 있게 되면 설계가 무너진다.**

```bash
node -e "const U=require('./src/roster.json');const C={hp:999999,en:999,atk:9999,def:9999,mob:9999,sct:9999};
let m=1e9;U.forEach(u=>{const n=Math.ceil((C.hp-u.hp)/100)+(C.en-u.en)+(C.atk-u.atk)+(C.def-u.def)+(C.mob-u.mob)+(C.sct-u.sct);if(n<m)m=n});
console.log('최소 필요',m,'· 4pt×9998 =',4*9998)"
```

### 상점 발주 조건

```js
const ORDER_MARKUP = 1.15;                                   // 웃돈
const orderDays = B => clamp(Math.round(uPow(B)/26000), 3, 20);  // 소요일
const rankReqOf = B => clamp(Math.floor(powPct(B)*RANKS.length), 0, RANKS.length-1);
```

계급 제한은 **성능 백분위**로 자른다. 고정 금액 구간은 쓰지 말 것
([§7.7-b](#77-b-상점-발주-5--6)).

### 난이도 조정

| 하고 싶은 것 | 만질 곳 |
|---|---|
| 전체적으로 쉽게 | `FOE_DMG` ↓ (0.55 → 0.45) |
| 전투를 짧게 | `DMG_K` ↑ (3.0 → 3.6) |
| 특정 임무만 | 해당 `MISSION.diff` |
| 명중 편차 줄이기 | `HIT_CLAMP` ↓ (6 → 4) |
| 무승부를 줄이기 | `ROUND_CAP` ↑ 또는 `FOE_HP` ↓ |
| 지휘기를 어렵게 | **`FOE_HP.boss` 가 아니라 보스 ATK 배수** ([§11.14](#1114-보스-hp-를-올리면-강해지는-게-아니라-안-끝난다)) |
| 저력을 자주 보고 싶다 | `GUTS_HP` ↑ (0.30 → 0.45) 또는 `gutsP` 곡선 |
| 스킬 영향력 조절 | `PSKILL` 의 계수, `PSK_SLOT`, `pskCost` |
| 거리 싸움 비중 | `RANGE_MUL`(사거리 환산) · `DIST_MAX` |
| 색적 비중 | `spotBon` / `hideBon` 의 나눗수 |

바꾼 뒤에는 §10 시뮬레이터로 확인할 것.

### 미래 예측 문턱 바꾸기

```js
const AWK_FORESEE   = 800;    // 발동 하한
const AWK_FORESEE_P = 0.05;   // 확률
const AWK_RANGE_CAP = 13;     // 각성 무장 사거리 상한
```

`awkOf()` 가 `st.awk + awkTmp` 를 보므로, **일시 보정으로 그 턴만 800 을 넘겨도
발동한다.** 스킬·파츠 효과를 넣을 자리는 `awkTmp` 하나뿐이다.

### 각성 무장 판정 기준

지금은 속성에 `특수` 가 들어가면 각성 무장이다.

```js
const wIsAwk = w => (w.at || '').indexOf('특수') >= 0;
```

이름으로 판정하고 싶다면(판넬·비트·사이코 등) 이 한 줄만 바꾸면 된다.
`roster.json` 재수집은 필요 없다.

### 도감·상점 페이지 크기

```js
const PAGE_N = 60;   // src/client.js — 한 페이지에 몇 기를 보여줄지
```

`pager(total, pg, tag)` 가 도감(`bookpg`)과 상점(`shoppg`) 양쪽을 그린다.
1244기를 한 번에 그리면 `#mainf` 가 448KB 였는데, 60기씩 끊으니 28KB 가 됐다.

### 이미지 크기 바꾸기

`--resize=<px>` (sharp 필요). 용량 표는 [4.4절](#44-이미지는-왜-분리하는가)에 있다.
화면 표시 크기는 `src/head.html` 의 네 단계이고, 어느 크기에 썸네일을 쓸지는
`src/client.js` 의 `picFor()` 가 정한다.

```css
.ui.s{width:64px;height:56px}     /* 전투판·도감 그리드 — 썸네일 */
.ui.m{width:124px;height:106px}   /* 목록 행 — 썸네일 */
.ui.l{width:100%;max-width:264px;height:212px}   /* 좌측 탑승기 — 초상화 원본 */
.ui.xl{width:100%;max-width:240px;height:206px}  /* 교전 표시부 — 초상화 원본 */
```

비율은 초상화 원본(936×803 ≒ 1.17:1)에 맞춰 잡았다. 썸네일은 128×128 정사각이라
`object-fit:contain` 으로 레터박스 처리된다.

### 기체명 접두어

`src/fetch-roster.mjs` 의 `NAME_PREFIX`. 기본값 `'[SD] '`. 빈 문자열로 두면 접두어가 사라진다.
로스터에 저장되는 값이므로 바꾼 뒤 **재수집이 필요하다.**

### 등급 표기를 되살리려면

`rar` 값은 데이터에 그대로 남아 있다(적 편성·EXP 계산에 쓰인다).
`src/client.js` 에 아래를 넣고 원하는 위치에 끼우면 된다.

```js
const rtag = r => '<span class="rtag r' + r + '">' + r + '</span>';
```

등급색 `--n/--r/--sr/--ssr/--ur` CSS 는 `head.html` 에 아직 남아 있다.
다만 **가격은 등급이 아니라 제원에서 나오므로** 되살려도 값과 등급이
정확히 일치하지는 않는다([§11.16](#1116-등급을-지웠으면-가격도-등급에서-뽑으면-안-된다)).

### 능력 포인트 곡선

```js
const START_PT = 120;   // 생성 시 자율 분배
const LVUP_PT  = 5;     // 레벨업 1회당
```

`START_PT + (LV_MAX − 1) × LVUP_PT` 가 `6 × STAT_MAX` 를 넘지 않아야
"레벨업만으로는 만렙 불가"라는 설계가 유지된다. 현재 5,110 < 5,994.

### 일일 행동력

`src/rules.mjs` 의 `AP_BASE`(기본 10). 4레벨마다 상한이 1씩 오르는 규칙은
`gainExp()` 안에 있다.

### 언어 바꾸기

`fetch-roster.mjs` 의 `LANG` 을 `JP`/`TW`/`HK` 로. **한국어는 API가 지원하지 않는다.**
무장·특성·시리즈는 `src/i18n.mjs` 가 빌드 시점에 옮기고([4.5절](#45-한글화)),
UI 문자열은 `head.html`·`rules.mjs`·`client.js` 에 한글로 하드코딩돼 있다.
`LANG` 을 바꾸면 사전 키가 안 맞으므로 사전도 함께 갈아야 한다.
`wIsAwk` 가 `'특수'` 를 문자열로 찾으므로 **속성 사전을 바꾸면 각성 무장 판정도 같이 손봐야 한다.**

### 세이브 스키마를 바꿀 때

세이브는 서버가 갖고 있고 `SAVEKEY` 는 없어졌다. 대신 **`rules.mjs` 의
`migrate()` 에 보정을 추가한다.** 저장소에서 꺼낼 때마다 통과하므로
필드를 더해도 옛 기록이 깨지지 않는다.

```js
function migrate(o) {
  ...
  if (!o.newField) o.newField = 기본값;
  return o;
}
```

`migrate()` 는 **로스터에서 사라진 기체도 걸러낸다.** 남은 기체가 없으면
`null` 을 돌려주고, 그러면 신규 등록 화면이 뜬다.

호환이 도저히 안 되는 변경이라면 저장소를 통째로 비우는 편이 낫다.

```bash
rm -rf data/                                   # 파일 저장소
# MongoDB: db.saves.deleteMany({})
```

### 서버 액션 추가

`src/actions.mjs` 의 `H` 에 핸들러를 하나 더한다. 클라이언트는
`act({ k: '이름', ... })` 만 보내면 된다.

```js
const H = {
  ...,
  scrap(g, a) {
    const v = R.cur();
    if (g.garage.length <= 1) no('마지막 기체는 해체할 수 없습니다.');
    ...
    return { msg: { t: '해체 완료', b: '...' } };
  }
};
```

**인자 이름이 `k` 와 겹치지 않게 할 것** ([§11.25](#1125-액션-이름과-액션-인자가-같은-키를-쓰면-안-된다)).
그리고 **여기서 `await` 를 쓰면 안 된다** ([§11.22](#1122-모듈-전역-상태를-서버에서-쓰려면-동기가-조건이다)).

### 저장소 백엔드 추가

`src/store.mjs` 가 `{ get, put, del, touch, close }` 다섯 개만 요구한다.
Redis·Postgres·S3 무엇이든 이 모양만 채우면 나머지는 손댈 것이 없다.

```js
async function redisStore(url) {
  ...
  return { kind:'redis', where:url,
    async get(pid){ ... }, async put(pid,g){ ... },
    async del(pid){ ... }, async touch(){}, async close(){} };
}
```

---

## 14. 출처와 권리

- **전투 규칙 계보** — FF ADVENTURE v0.45 (D.Takamiya / CUMRO) → MSVS →
  MS Tactics v1 (LAK). 2d6 대항판정이라는 뼈대만 가져왔고 코드는 전부 새로 썼다.
- **기체 제원·무장·이미지** — [GGen Eternal Database](https://ggendb.up.railway.app/u).
  푸터에 출처를 표기한다. 이미지를 내장하는 대신 원 서버에 반복 요청을 보내지 않는다.
- 원작 IP는 반다이남코 / 선라이즈에 있다. **비영리 팬 제작물**이며 배포·판매하지 않는다.
- 로스터를 대량으로 늘릴 때는 상대 서버 부담을 생각해 동시 요청 수
  (`pool(items, 6, ...)` / `pool(items, 8, ...)`)를 그대로 두는 것을 권한다.

---

## 부록 — 함수 색인

**rules.mjs — 상태 · 파생** (서버·클라 공용)

| 함수 | 역할 |
|---|---|
| `uStat(v)` | 개조 반영된 기체 제원 (HP·EN·공격·방어·기동·색적, UCAP 로 clamp) |
| `adaptOf(v,k)` / `adaptBase(B,k)` | 파츠 포함 / 기본 지형 적성 (0=× 1=△ 2=○) |
| `canSortie(v,k)` | 그 전장에 출격 가능한가 |
| `wpowOf(v,i)` | 무장 레벨 반영 위력 |
| `uPow(B)` / `buyPrice(B)` / `lvReqOf(B)` | 제원 총합 → 시세 → 요구 레벨 |
| `marketOf(day)` | 날짜 시드 암시장 재고 (1~10기) |
| `trainTier(v)` | 훈련 성공률 구간 |
| `skillEffect(ctx)` | 장착 스킬 → 효과 객체 집계 |
| `effStats(B)` | 스킬을 반영한 실효 능력치 + `penMul` (화면·전투 공용) |
| `penMulOf(spi, e)` | 최종 패널티 배수 |
| `gutsP(spi)` | 저력 발동 확률 |
| `pskCost(lv)` / `pskLv(k)` / `pskEquipped(k)` | 스킬 비용 · 보유 레벨 · 장착 여부 |
| `uBase(v,k)` / `uInvMaxed(v,k)` | 기체 레벨 투자분 포함 소재값 · 상한 도달 여부 |
| `uExpNeed(lv)` / `gainUnitExp(v,n)` | 기체 EXP 곡선 · 기체 레벨업 |
| `orderPrice(B)` / `orderDays(B)` / `rankReqOf(B)` / `rankIdx()` | 상점 발주 조건 |
| `statTotal()` | 캐릭터 능력치 합계 — 적 능력 산출의 기준 |
| `gainExp(n)` | EXP 가산 · 레벨업 · 능력 포인트 지급 |
| `initRules(units)` | 로스터 주입 — `UNITS`·`UMAP`·`SERIES_LIST`·`POW_SORTED` 구성 |
| `setState(g)` / `getState()` | 처리 중인 세이브 교체 · 조회 |
| `migrate(g)` | 저장소에서 꺼낸 세이브의 누락 필드 보정 · 무효 판정 |
| `newGame(nm, st, id)` | 새 세이브 생성 (저장은 하지 않는다) |

**rules.mjs — 전투** (서버 전용 · 완전 동기)

| 함수 | 역할 |
|---|---|
| `mkPlayer(tac, ter)` | 전투용 플레이어 유닛 (전장 계수 반영) |
| `mkFoe(B, elite, boss, idx, diff, ter)` | 전투용 적 유닛 |
| `buildFoes(ms, ter)` | 임무 + 전장 → 적 편성 |
| `wRange(u,w)` / `inRange(u,w,d)` | 거리축 사거리 (각성 보정 포함) |
| `armed(u,d)` | 이 거리에서 쓸 무장이 있는가 |
| `wantBand(u)` / `wantDir(u,d,iCan,foeCan)` | 거리 싸움의 희망 구간·방향 |
| `mvSpd(u)` | 거리 조정 속도 |
| `spotBon(u)` / `hideBon(u)` | 색적 · 은신 보정치 |
| `awkOf(u)` / `foresee(u)` | 실효 각성치 / 미래 예측 발동 판정 |
| `advForP(t)` / `advScale(adv,mul)` | 확률 → 우열 역변환 (저력의 명중률 ×1.30) |
| `isGundamName(B)` | 기체명에 '건담/Gundam' 이 들어가는가 (건담 마이스터) |
| `wIsAwk(w)` / `wIsMelee(w)` | 각성 무장 / 격투 무장 판별 |
| `usable(u,w)` | EN · 탄약 · 각성 조건 |
| `hitAdv(A,D,w)` | 명중 우열 (clamp 포함) |
| `critNeed(A,D,w)` | 크리티컬 필요 마진 |
| `calcDmg(A,D,w)` | 피해량 |
| `chooseWep(A,D,dist)` | 기대 피해 최대 무장 선택 |
| `resolveBattle(ms,tac,ter)` | 전투 전체 계산 + 정산 → `{ cast, ev, res }` (**동기**) |

| `oppP(adv)` | 2d6 대항판정 명중 확률표 |

**client.js** (브라우저 전용)

| 함수 | 역할 |
|---|---|
| `renderAll()` | 좌측 + 메인 + 커맨드 바 재렌더 |
| `view*()` | 화면별 HTML 생성 (`viewMain` … `viewMarket` … `viewLog`) |
| `allocTable` / `bindAlloc` | 능력 포인트 분배 위젯 (생성 · 레벨업 공용) |
| `terLine(src, owned)` | 지형 적성 5칸 표시 |
| `statLine(s)` | 기체 능력치 6항목 한 줄 |
| `wepDetail(w, pw, awk)` | 무장 상세 (거리축 사거리 · 각성 표기) |
| `picFor(B, cls)` | 표시 크기에 맞는 이미지 선택 (썸네일 / 초상화) |
| `pager(total, pg, tag)` | 도감·상점 페이지 넘김 위젯 |
| `openZoom(src, cap)` / `zoomAttr(B)` | 이미지 확대 오버레이 |
| `allocTable` / `bindAlloc` (파일럿) · `uAlloc` 계열 (기체) | 두 종류의 포인트 배분 |
| `awkLocked()` | 올드타입 장착 시 각성 잠금 |
| `bindMain()` | 메인 프레임 이벤트 위임 |
| `act(a)` | 액션을 서버로 보내고 결과를 반영 (거절 사유도 화면에) |
| `post(url,b)` / `getJSON(url)` / `adopt(j)` | 통신 · 응답 반영 |
| `playBattle(b)` | 서버가 보낸 이벤트를 원래 속도로 재생 |
| `paintDuel` / `paintBoard` / `bl` | 교전 표시 · 상황판 · 로그 |
| `viewCode()` | 이어하기 코드 화면 |
| `nextDay()` | `act({k:'nextday'})` 호출 |

**actions.mjs** (서버 전용)

| 함수 | 역할 |
|---|---|
| `applyAction(g, a)` | 액션 라우팅 + 상태 교체/복원. 거절은 `Reject` 로 던진다 |
| `H.*` | 액션별 핸들러 20종 (`new` `train` `alloc` `ualloc` `skbuy` `skeq` `skuneq` `sortie` `repair` `mod` `wl` `partbuy` `partfit` `partoff` `mktbuy` `order` `ordercancel` `ride` `sell` `nextday`) |
| `no(msg)` | 거절 — 서버가 400 과 함께 이 문장을 돌려준다 |
| `intMap(src, keys)` | 클라이언트가 보낸 정수 맵을 화이트리스트로 걸러 받는다 |

**api.mjs · store.mjs · server.mjs** (서버 전용)

| 함수 | 역할 |
|---|---|
| `handleApi(req,res,path)` | `/api/state` `/api/act` `/api/login` `/api/reset` |
| `readPid` / `setCookie` / `newPid` | 플레이어 코드 (CSPRNG 24자리 16진수) |
| `readBody(req)` | JSON 본문 파싱 · 64KB 상한 |
| `openStore()` | `MONGODB_URI` 유무로 mongo / file 선택 · 연결 캐시 |
| `serveStatic` | `docs/` 정적 서빙 · 이미지에 1년 불변 캐시 |
